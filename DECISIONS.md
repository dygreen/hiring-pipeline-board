# DECISIONS.md — 설계 결정과 트레이드오프

구현하며 내린 판단 중 다르게 갈 수 있었던 것들과, 그렇게 정한 이유를 남긴다.
자세한 검증 과정은 [`PROMPTS.md`](./PROMPTS.md)에 있다.

---

## 1. 낙관적 업데이트 — 공식 레시피의 전체 스냅샷 롤백을 쓰지 않았다

**결정**: 되돌릴 대상을 목록 전체 스냅샷이 아니라 `{ 카드 id, 되돌아갈 단계 }`로 좁혔다.

TanStack Query 공식 낙관적 업데이트 레시피는 `onMutate`에서 캐시 전체를 스냅샷으로 잡고
`onError`에서 통째로 되돌린다. **동시에 실행되는 mutation이 하나일 때만 맞다.**

이 화면은 아니다. 지연이 200~800ms이고 15%가 실패하므로,
사용자가 응답을 기다리지 않고 계속 조작하는 것이 전제다. 같은 카드를 빠르게 두 번 옮기면:

```
1) 서류검토 → 면접    낙관 반영.  스냅샷 A = [서류검토]
2) 면접 → 처우협의    낙관 반영.  스냅샷 B = [면접]     ← 이미 낙관 변경된 상태를 잡는다
3) 1)이 실패          스냅샷 A로 복원 → 서류검토
```

두 번째가 성공했는데도 카드가 **두 단계 전**으로 간다.
게다가 스냅샷이 목록 전체라 **그 사이 다른 카드에 일어난 변경까지 되돌린다.**

**트레이드오프**: 직접 관리하는 상태가 늘었다. 대신 무엇을 되돌리는지가 명시적이다.

### `onSettled` 무효화도 하지 않는다

흔한 구성은 `onSettled`에서 목록을 무효화해 서버와 다시 맞추는 것이다. 여기서는 두 가지가 걸린다.
이동 한 번마다 1,000건을 다시 받고, 그 응답이 도착하는 사이 사용자가 또 조작하면
**늦게 온 목록이 최신 낙관 상태를 덮어쓴다.**
서버가 이동 결과로 갱신된 카드를 돌려주므로 그 카드만 확정본으로 교체한다.

**트레이드오프**: 다른 사용자가 바꾼 내용은 새로고침 전까지 반영되지 않는다.
이 과제는 단일 사용자 화면이라 감수했다.

---

## 2. 경쟁 상태 — 세 겹으로 나눠 풀었다

**결정**: 직렬 큐 · 요청 순번 · 확정 단계 기준 롤백을 각각 두었다.
`src/features/board/moveQueue.ts`에 React와 무관한 모듈로 분리했다.

하나로 뭉뚱그리면 어느 장치가 무엇을 막는지 흐려진다.

| 겹 | 막는 것 |
|---|---|
| **직렬 큐** | 서버 도착 순서가 사용자 클릭 순서와 달라지는 것. 같은 카드만 줄 세우고 다른 카드는 병렬 |
| **요청 순번(seq)** | 큐를 통과해도, 응답 처리 시점에 이미 지나간 요청이 화면을 되돌리는 것 |
| **확정 단계 기준 롤백** | 서버에 저장된 적 없는 단계로 되돌아가는 것 |

**서버의 `revision` 대신 클라이언트 순번을 쓴 이유**:
`revision`은 서버가 갱신에 성공할 때만 올라간다. **실패한 요청은 revision을 올리지 않으므로**
서버 값만으로는 "실패한 옛 요청"을 구분할 수 없다. 문제가 되는 것이 정확히 그 요청이다.

**"확정 단계"를 진행 중인 요청이 없을 때만 갱신하는 이유**:
요청이 겹친 상태에서 두 요청이 모두 실패하면, 두 번째의 "직전 단계"는 첫 번째가 만든 낙관 값이다.
그걸로 되돌리면 **서버에 한 번도 저장된 적 없는 단계**가 화면에 남는다.

React에서 분리한 것은 테스트 때문이다. 경쟁 상태는 화면으로 재현하기 어렵다 —
지연이 랜덤이고 원하는 순서를 만들려면 응답 시각을 통제해야 한다.

---

## 3. 드래그앤드롭 대신 액션 버튼 + 키보드

**결정**: 카드에 이동 버튼을 두고, 키보드는 방향키로 조작한다. DnD는 구현하지 않았다.

요구사항이 둘 중 택1인데, **DnD를 택하면 키보드 대체 경로가 필수로 따라온다.**
즉 DnD를 택해도 버튼/키보드 경로는 어차피 만들어야 한다.

액션 버튼을 1차 수단으로 두면 키보드 조작이 UI의 우회로가 아니라 **같은 경로**가 되고,
접근성이 나중에 덧붙이는 기능이 아니라 기본 동작이 된다.
DnD 라이브러리에 상태 로직을 넘기지 않는다는 이점도 있다.

**트레이드오프**: 칸반 보드로서의 직관성은 DnD가 낫다.

---

## 4. 단계 이동 규칙 — 요구사항에 없어 가정했다

요구사항에 단계 간 이동 제약이 명시되어 있지 않아 아래와 같이 가정하고 구현했다.

```
서류검토 ⇄ 면접 ⇄ 처우협의 ⇄ 최종합격     진행·후퇴는 한 칸씩
     ↓      ↓       ↓        ↓
            불합격                        어느 단계에서든 가능
불합격 → 서류검토                         복원은 처음으로만
```

| # | 가정 | 근거 |
|---|---|---|
| 1 | 정상 경로는 앞뒤 한 칸씩 | 실제 채용 프로세스와 가깝고 `←→` 키 조작과 자연스럽게 맞는다. **이동 불가 상태가 생겨야** 버튼 비활성화와 그때의 포커스 거동을 검증할 수 있다 |
| 2 | 불합격은 어느 단계에서든 가능 | 탈락은 어느 시점에서든 일어난다 |
| 3 | 불합격에서의 복원은 서류검토로만 | 복원 지점을 여러 개 두려면 "어느 단계에서 탈락했는지"를 서버가 기억해야 한다. 이 과제 범위 밖 |
| 4 | **되돌리기는 이 규칙을 타지 않는다** | 되돌리기는 새 이동이 아니라 **이미 있었던 상태로의 복원**이다. 규칙으로 막으면 되돌릴 수 없는 이동이 생긴다 (면접 → 불합격을 되돌리면 규칙상 건너뛰기) |
| 5 | 검색·필터는 클라이언트에서 처리 | mock API가 전체를 내려주는 구조이고 1,000건은 클라이언트가 감당 가능하다. 서버 페이지네이션까지 흉내내면 이 과제의 핵심(낙관적 업데이트·경쟁 상태)에서 초점이 흐려진다 |

규칙은 `stageRules.ts`에 함수로 두었다.
"이동 가능한가"와 "다음은 어디인가"는 **화면(버튼 활성화)·키보드·되돌리기** 세 군데에서 필요한데,
각자 판단하게 두면 셋이 어긋난다.

**한계**: 규칙은 클라이언트에만 있다. mock API는 여전히 어떤 단계로든 이동을 허용한다.
실제 서버라면 서버가 검증해야 한다.

---

## 5. mock API — MSW, localStorage 백킹, 작업별 실패율

**MSW를 고른 이유**: `npm run dev` 하나로 실행되고 **배포된 정적 사이트에서도 그대로 동작한다.**
json-server는 별도 프로세스가 필요해 확인 비용이 늘어난다.

**localStorage 백킹**: MSW 핸들러는 프로세스 메모리에서 돌아 새로고침하면 초기화된다.
요구사항은 "이동이 mock API에 저장되어 새로고침 후에도 유지"되는 것이므로,
메모리를 1차 저장소로 쓰되 변경마다 localStorage에 미러링한다.
매 요청마다 1,000건을 `JSON.parse` 하지 않기 위해 메모리를 거친다.

**시드를 고정 난수로 만든 이유**: `Math.random`을 쓰면 새로고침할 때마다 이름과 지원일이 바뀐다.
"아까 그 카드"를 다시 찾을 수 없고 검증도 재현되지 않는다.

**단계 분포를 균등이 아니라 가중치로 둔 이유**(45/22/8/10/15):
균등 분배하면 컬럼마다 200건씩 쌓여서, **가상 스크롤이 필요한 상황(한 컬럼에 수백 건이 몰리는 것)이
아예 만들어지지 않는다.**

**조회와 이동의 실패율을 분리한 이유**: `?failRate=1`은 목록 조회에도 적용되어
**카드가 없는 화면**이 된다. 확인하려는 것은 "이동이 실패했을 때 카드가 제자리로 돌아오는가"인데
그러려면 카드가 먼저 보여야 한다. → `?moveFailRate=1`

**프로덕션 번들에 mock이 포함된다.** 배포본에도 백엔드가 없어 mock이 실제로 동작해야 하기 때문이다.
실제 백엔드가 생기면 통째로 걷어낼 부분이라 동적 import로 분리해 경계를 드러냈다
(index 273KB / browser 418KB).

---

## 6. AI 협업 방식 — 검증 강도를 나누고, 리뷰는 다른 벤더에게 맡겼다

`CLAUDE.md`에 작업 규칙을, `PROMPTS.md`에 기능별 기록을 남겼다.

**검증 강도를 기능마다 다르게 뒀다.** 전부에 같은 검증을 도는 것은 비용만 늘고,
기록이 "리뷰했음"의 나열이 되어 오히려 형식적이 된다.
낙관적 업데이트·경쟁 상태·접근성·가상 스크롤은 풀 검증, 레이아웃·카드는 코드 정독으로 끝냈다.

**교차 리뷰를 같은 모델이 아니라 `codex`(다른 벤더 CLI)에 맡겼다.**
같은 모델 계열에게 자기가 만든 걸 리뷰시키면 자기 편향이 생긴다. 자기가 놓친 건 다시 봐도 놓친다.

실제로 효과가 있었다.

| 커밋 | codex가 잡은 것 |
|---|---|
| 4 | `new Date('2026-08-01')`이 UTC로 파싱되어 UTC-7 지역에서 날짜가 하루 밀리고 **연말·연초엔 연도까지 틀림** (`2026-01-01` → `2025년 12월 31일`) |
| 13 | 카드 이동 후 **포커스 복원 장치가 없음**. 상세 패널만 고치고 보드는 같은 문제가 남아 있었다 |

**한계**: codex는 **불안정했다.** 커밋 4 성공(2분) → 커밋 9 두 번 실패(18분 무응답, 5분 초과) → 커밋 13 성공.
작업 규칙에 적어둔 "같은 시도 3회 실패 시 중단"에 따라 커밋 9에서는 멈추고 실행 검증으로 대체했다.
프롬프트를 짧게 하고 대상 파일을 2개 이하로 좁히면 성공률이 높아 보였지만, 확실하지 않다.

**"완료했습니다"를 믿지 않고 실제로 눌러봤다.**
매 커밋 Playwright로 화면을 띄워 조작했고, 특히 실패율을 100%로 강제해 롤백을 눈으로 확인했다.
이 과제에서 이 원칙이 특히 중요했던 이유가 있다 —
**낙관적 업데이트는 "UI가 됐다고 말하는데 서버는 실패한 상태"를 의도적으로 만드는 기능**이라,
코드만 읽어서는 맞는지 알 수 없다.

**테스트가 실제로 버그를 잡는지도 확인했다.** 구현을 일부러 망가뜨려 각 테스트가 실패하는지 봤고,
그 과정에서 빈틈을 하나 찾아 테스트를 추가했다(최종 상태만 보는 테스트는 "잠깐 잘못된 화면"을 놓친다).

---

## 7. 알려진 한계

구현하지 않았거나 불완전한 부분을 적는다.

| 항목 | 내용 |
|---|---|
| **드래그앤드롭** | 구현하지 않았다. 요구사항의 택1에서 액션 버튼을 골랐다(§3) |
| **스크린리더 실검증** | VoiceOver·NVDA로 직접 들어보지 못했다. 접근성 트리·포커스 이동·`aria-live` DOM으로만 확인했다. `role="status"`가 방해 없이 읽히는지, `role="alert"`가 제때 끼어드는지는 미확인 |
| **가상 스크롤 행 높이** | 116px로 하드코딩했다. 이름이나 직무가 길어 카드가 두 줄이 되면 위치 계산이 어긋난다. 현재 데이터는 한 줄로 잘리게 되어 있어 문제없지만 `measureElement`는 적용하지 않았다 |
| **되돌리기 후 스크롤** | 되돌린 카드가 화면 밖(가상 스크롤)에 있어도 그 위치로 스크롤·포커스가 이동하지 않는다. 토스트 버튼으로 되돌리면 카드가 조용히 제자리로 간다 |
| **되돌리기 깊이** | 1건만 기억한다. 요구사항이 "방금 한 이동"이고, 스택으로 쌓으면 그 사이 다른 조작이 있었을 때 "이 시점으로 되돌린다"가 애매해진다 |
| **이동 규칙의 위치** | 클라이언트에만 있다. mock API는 어떤 단계로든 허용한다(§4) |
| **다중 사용자** | 다른 사용자의 변경은 새로고침 전까지 반영되지 않는다(§1) |
| **좁은 화면** | 375px에서 컬럼이 1.4개만 보이고 나머지는 가로 스크롤이다(칸반 보드의 표준 동작). 단계 탭으로 한 컬럼씩 보여주는 방식은 검토하지 않았다 |
| **컴포넌트 테스트** | 버튼 비활성화·토스트 표시 등 화면 표시는 테스트로 고정하지 않았다. 눈으로 확인하기 어려운 순서·롤백 로직을 우선했고, 화면 표시는 매 커밋 실행 검증으로 확인해 왔다 |
