# PROMPTS.md — 기능별 프롬프트 & 리뷰 로그

AI와 주고받은 프롬프트와 그에 대한 리뷰·검증 기록. 커밋과 1:1로 대응한다.

**기록 원칙**
- 프롬프트는 요약이 아니라 실제로 준 문구를 적는다.
- `리뷰 / 검증`에는 **실제로 확인한 것만** 적는다. 확인하지 않았으면 그렇게 적는다.
- 길이가 아니라 판단의 밀도를 남기는 것이 목적이라, 1차 출력을 그대로 채택한 기능은 짧다.

**사용 도구**: Claude Code (구현) / Codex CLI (교차 리뷰) / Playwright MCP (실행 검증)

---

## [rules] AI 협업 규칙 문서 — CLAUDE.md · AGENTS.md · PROGRESS.md

### 배경

과제 분석과 계획 수립을 마친 뒤, 구현을 시작하기 전에 규칙 문서를 먼저 만들기로 했다.
이유는 세션이 끊기면 규칙도 함께 사라지기 때문이다.

LLM의 실패 유형 중 **컨텍스트 손실** — 긴 작업 도중 초기 지시를 잊는 것 — 은
대화를 이어가는 것으로는 막을 수 없다. 대화가 끝나면 초기화되기 때문이다.
그래서 지시를 **파일에 고정**해 두고, 새 세션이 그 파일부터 읽게 만든다.

세션이 바뀌며 규칙이 흔들리면 같은 문제를 저장소 안에서 서로 다른 방식으로 풀게 된다.
나중에 읽는 사람은 어느 쪽이 의도된 방식인지 알 수 없다.

### 프롬프트 1

> 다른 환경에서도 해당 레포지토리에 ai 가 접근했을 때, 바로 이해할 수 있는 규칙을 담은 md 문서도 함께 만들어줘.
> 이 컨텍스트가 끝나고 다른 컨텍스트를 열어서 작업을 재개할 때도 동일한 규칙을 가지고 작업을 할 수 있는 환경을 만들 수 있게 말이야.

### AI 출력 요지

`CLAUDE.md`(정본) / `AGENTS.md`(Codex·Cursor 진입점) / `PROGRESS.md`(진행 상태) 3종 구성을 제안.
`CLAUDE.md`에 평가 기준·절대 금지 항목·아키텍처 경계·검증 절차·커밋 규약을 담고,
나머지 둘은 얇게 유지하는 구조.

### 리뷰 / 검증

**규칙 문서를 두 벌로 유지하자는 초안을 기각했다.**
`CLAUDE.md`와 `AGENTS.md`에 같은 내용을 각각 담으면 한쪽만 고쳐질 때 반드시 어긋난다.
그 시점에는 어느 쪽이 정본인지 알 수 없게 된다.
→ 정본은 `CLAUDE.md` 하나로 두고, `AGENTS.md`는 **포인터 + 자주 잘못 건드리는 것 3가지**만 남겼다.

**금지 항목을 명시적으로 넣도록 요구했다.**
규칙 문서는 결국 프롬프트라서, "이렇게 해라"보다 "이건 하지 마라"가 더 안전하게 작동한다.
특히 `rebase -i` / `squash` / `--amend` / `push --force`는 이 과제에서 **제출물을 파괴하는** 명령이다.
커밋 히스토리가 제출물의 일부인데 AI가 무심코 히스토리를 정리하면 되돌릴 수 없다.

**`moveQueue.ts`를 라이브러리로 대체하지 말라는 조항을 직접 추가했다.**
아직 존재하지 않는 파일이지만, 나중에 다른 세션의 AI가 코드를 보면
"TanStack Query 훅으로 단순화할 수 있다"고 제안할 것이 예상된다.
그 제안이 왜 틀렸는지(연속 이동 시 스냅샷이 이미 오염되어 있다는 것)를 **미리 근거와 함께** 적어두었다.
리팩터링 제안을 그때 가서 방어하는 것보다, 근거를 문서에 박아두는 편이 확실하다.

**계획 문서(`docs/PLAN.md`)는 그대로 싣지 않고 다시 썼다.**
초안은 평가 항목을 어떻게 공략할지의 관점으로 쓰여 있었고, 로컬 절대 경로와 개인 파일 참조가 섞여 있었다.
저장소에 남길 문서는 "어떻게 잘 보일까"가 아니라 "무엇을 왜 이렇게 정했나"를 적는 자리라고 판단해,
**요구사항 분해 / 기술 선택 근거 / 난이도가 걸린 지점 / 커밋 순서** 중심으로 재작성했다.

**검증하지 않은 것**: 실제로 새 세션을 열어 이 문서만 읽고 작업이 이어지는지는 아직 확인하지 못했다.
구현이 몇 단계 진행된 뒤 확인하고 결과를 여기 추가한다.

---

## [setup] 프로젝트 초기화

**검증 강도**: 경량 (구현 → 코드 정독)

### 프롬프트 1

> 커밋 1 `chore(setup)` 진행. Vite + React 19 + TS 초기화, 린트, 폴더 구조, README 뼈대.
> 의존성은 필요한 커밋에서 각각 추가할 것 — MSW는 커밋 2, TanStack Query는 커밋 7, react-virtual은 커밋 14.

### AI 출력 요지

`npm create vite`로 react-ts 템플릿 생성 후, Vitest·Testing Library·Prettier를 추가하고
`src/{features,components,mocks,types,lib,test}`로 폴더 구조를 정하고, `src/types/candidate.ts` 도메인 타입과 README 뼈대 작성.

### 리뷰 / 검증

**의존성을 한 번에 다 깔지 않기로 한 것은 의도적이다.**
초기 설정에서 필요한 패키지를 전부 설치하면 편하지만, 커밋 히스토리만 봐서는
어떤 라이브러리가 어떤 문제 때문에 들어왔는지 알 수 없다.
MSW는 mock API를 만들 때, TanStack Query는 낙관적 업데이트를 붙일 때 각각 들어오게 했다.

**`npm run build`가 실패했다.** `vite.config.ts`에서 `test` 키를 인식하지 못했다.

```
vite.config.ts(6,3): error TS2769: No overload matches this call.
  Object literal may only specify known properties, and 'test' does not exist in type 'UserConfigExport'
```

원인은 `defineConfig`를 `vite`에서 import한 것이었다. Vitest 4는 `vitest/config`의 `defineConfig`를 써야
`test` 키가 타입에 포함된다. AI 초안이 `vite`에서 가져오고 있었다.
→ import 출처를 `vitest/config`로 교체.

이어서 `/// <reference types="vitest/config" />`를 함께 넣었는데 린트가 잡았다.
`vitest/config`에서 직접 import하면 타입이 이미 잡히므로 삼중 슬래시 참조는 **불필요한 중복**이었다.
→ 삭제. 삭제 후 빌드·린트 모두 통과하는 것을 확인했다.

**`npm test`가 exit 1로 실패하는 것을 발견했다.** 아직 테스트 파일이 없어서다.
테스트는 커밋 10에서 들어오는데 그때까지 `npm test`가 빨간불이면
"원래 실패하는 것"으로 취급하게 되어 진짜 실패를 놓친다.
→ `--passWithNoTests`를 붙였다. 테스트가 생긴 뒤에도 동작이 달라지지 않으므로 그대로 둔다.

**`STAGES` 배열 순서에 주석으로 계약을 명시했다.**
이 배열은 단순한 목록이 아니라 **보드 컬럼 순서이자 이동 순서**를 동시에 정의한다.
나중에 누군가 알파벳순으로 정렬하면 화면과 이동 로직이 같이 깨진다. 주석 한 줄로 의도를 고정했다.

**스캐폴드 잔재를 정리했다.** Vite 템플릿의 `public/icons.svg`와 `src/App.css`, `src/assets/`는
기본 App.tsx를 교체하면서 참조가 사라졌다. 쓰이지 않는 파일이 저장소에 남으면
나중에 읽는 사람이 "어디선가 쓰이겠지"라고 생각하게 된다. → 삭제.
`public/favicon.svg`는 `index.html`이 참조하므로 남겼다.

**검증한 것**: `npm run typecheck` / `npm run build` / `npm run lint` / `npm test` 전부 통과.
**검증하지 않은 것**: 화면 렌더는 아직 제목뿐이라 브라우저 확인을 생략했다. 보드가 그려지는 커밋 3에서 확인한다.

---

## [mock-api] mock API — MSW 핸들러 · 저장소 · 시드

**검증 강도**: 중 (구현 → 실행 검증)

### 프롬프트 1

> 커밋 2 `feat(mock-api)` 진행. MSW 핸들러 + localStorage 백킹 + seed 1,000건 + 지연 200~800ms / 실패 15%.
> 요구사항상 이동이 mock API에 저장되어 새로고침 후에도 유지되어야 한다.
> 실패 동작을 확인하려면 15% 확률을 기다릴 게 아니라 강제할 수 있어야 한다.

### AI 출력 요지

`config.ts`(지연·실패율 + URL 쿼리 오버라이드) / `seed.ts`(1,000건 생성) / `db.ts`(localStorage 백킹) /
`handlers.ts`(GET 목록, PATCH 단계) / `browser.ts` · `server.ts` · `start.ts` 구성.

### 리뷰 / 검증

**시드를 `Math.random`으로 만든 초안을 고쳤다.**
새로고침할 때마다 이름과 지원일이 바뀌면 "아까 그 카드"를 다시 찾을 수 없다.
검증 중에 카드를 특정해야 하는 상황이 계속 생기는데 그때마다 대상이 달라진다.
→ 고정 시드 PRNG(mulberry32)로 교체했다. 재시드해도 항상 `c-0001 조하윤 / 백엔드 개발자`가 나온다.

**단계 분포를 균등에서 가중치로 바꿨다.**
초안은 5단계에 균등 분배해서 컬럼마다 200건씩 쌓였다. 실제 채용 파이프라인은 뒤로 갈수록 줄어들고,
무엇보다 **가상 스크롤이 필요한 상황(한 컬럼에 수백 건이 몰리는 것)이 드러나지 않는다.**
→ 서류검토 45 / 면접 22 / 처우협의 8 / 최종합격 10 / 불합격 15 가중치로 변경.
실제 생성 결과 457 / 198 / 74 / 112 / 159 로 의도대로 나왔다.

**늦게 도착한 응답을 시각으로 판별하려던 것을 `revision` 숫자로 바꿨다.**
초안은 `updatedAt` ISO 문자열을 비교해 최신 응답을 가리려 했다.
그런데 같은 밀리초 안에 두 번 갱신되면 두 응답의 `updatedAt`이 같아져서 순서를 가릴 수 없다.
빠른 연속 이동은 이 프로젝트에서 **일부러 만들어낼 상황**이라 실제로 걸린다.
→ 서버가 갱신할 때마다 1씩 올리는 `revision: number`를 추가했다.
`updatedAt`은 상세 화면 표시용으로 남겼다.

**`InvalidStage` 검증을 네트워크 시뮬레이션 앞으로 옮겼다.**
초안은 지연·실패를 먼저 태우고 body를 검증했다. 잘못된 요청은 서버에 닿기 전에 걸러지는 것이므로
800ms를 기다렸다가 400을 내는 건 실제 동작과 다르다. → 검증을 먼저, 지연은 그 뒤로.

**저장 실패를 조용히 넘기지 않게 했다.**
`writeStorage`의 `catch`를 초안은 빈 블록으로 뒀다. 용량 초과로 저장에 실패하면
화면은 정상으로 보이지만 새로고침하는 순간 변경이 사라진다. 원인을 찾기 어려운 종류의 실패다.
→ `console.warn`으로 남겼다.

**손상된 데이터와 스키마 버전 불일치를 처리했다.**
`JSON.parse`가 실패하면 앱 전체가 죽는다. 저장소에 뭐가 들어있을지는 통제할 수 없으므로
파싱 실패·버전 불일치·배열 아님을 모두 "다시 시드"로 처리했다.

**실행 검증** (Playwright, `npm run dev`):

| 확인 | 결과 |
|---|---|
| 목록 1,000건 조회 | 457 / 198 / 74 / 112 / 159 — 가중치대로 |
| PATCH 성공 | 200, `stage: offer`, `revision: 0 → 1` |
| localStorage 미러링 | 저장된 값도 `offer` / `revision 1` |
| **새로고침 후 유지** | 재조회 시 `offer` / `revision 1` 유지 |
| 실패 주입 `?failRate=1` | 503 `MOVE_FAILED`, **서버 상태 안 바뀜**(`revision 0` 유지) |
| 잘못된 단계 | 400 `INVALID_STAGE` |
| 지연 분포 | 279 / 802 / 463 / 694 / 381 / 800 ms |
| `?delay=0` 오버라이드 | 61ms (1,000건 직렬화 비용) |
| 손상된 localStorage | 1,000건으로 재시드, 스키마 버전 1 복구 |
| 콘솔 | 에러 3건 전부 위 테스트가 의도적으로 낸 503/400. 예기치 않은 에러 없음 |

지연이 802ms로 상한을 4ms 넘겼다. `delay()` 자체의 오차이며 요구 범위 해석상 문제 없다고 판단했다.

**검증 중 알게 된 것**: 실행 중에 localStorage를 지워도 목록이 그대로 나온다.
`db.ts`가 메모리를 1차 저장소로 쓰고 localStorage는 미러이기 때문이다.
실제 서버도 실행 중에 DB 파일을 지운다고 즉시 반영되지 않으므로 버그는 아니지만,
**저장소 관련 검증은 반드시 새로고침을 끼고 해야 한다**는 것을 확인했다.

**검증하지 않은 것**: 동시에 여러 PATCH를 보내는 상황은 확인하지 않았다.
경쟁 상태는 클라이언트 쪽 과제이므로 커밋 9에서 다룬다.
