# PROMPTS.md — 기능별 프롬프트 & 리뷰 로그

AI와 주고받은 프롬프트와 그에 대한 리뷰·검증 기록. 커밋과 1:1로 대응한다.

**기록 원칙**
- 프롬프트는 요약이 아니라 실제로 준 문구를 적는다.
- `리뷰 / 검증`에는 **실제로 확인한 것만** 적는다. 확인하지 않았으면 그렇게 적는다.
- 길이가 아니라 판단의 밀도를 남기는 것이 목적이라, 1차 출력을 그대로 채택한 기능은 짧다.

**사용 도구**: Claude Code (구현) / Codex CLI (교차 리뷰) / Playwright MCP (실행 검증)

---

## [rules] AI 협업 규칙 문서 — CLAUDE.md · AGENTS.md · PROGRESS.md

### 배경

과제 분석과 계획 수립을 마친 뒤, 구현을 시작하기 전에 규칙 문서를 먼저 만들기로 했다.
이유는 세션이 끊기면 규칙도 함께 사라지기 때문이다.

LLM의 실패 유형 중 **컨텍스트 손실** — 긴 작업 도중 초기 지시를 잊는 것 — 은
대화를 이어가는 것으로는 막을 수 없다. 대화가 끝나면 초기화되기 때문이다.
그래서 지시를 **파일에 고정**해 두고, 새 세션이 그 파일부터 읽게 만든다.

세션이 바뀌며 규칙이 흔들리면 같은 문제를 저장소 안에서 서로 다른 방식으로 풀게 된다.
나중에 읽는 사람은 어느 쪽이 의도된 방식인지 알 수 없다.

### 프롬프트 1

> 다른 환경에서도 해당 레포지토리에 ai 가 접근했을 때, 바로 이해할 수 있는 규칙을 담은 md 문서도 함께 만들어줘.
> 이 컨텍스트가 끝나고 다른 컨텍스트를 열어서 작업을 재개할 때도 동일한 규칙을 가지고 작업을 할 수 있는 환경을 만들 수 있게 말이야.

### AI 출력 요지

`CLAUDE.md`(정본) / `AGENTS.md`(Codex·Cursor 진입점) / `PROGRESS.md`(진행 상태) 3종 구성을 제안.
`CLAUDE.md`에 평가 기준·절대 금지 항목·아키텍처 경계·검증 절차·커밋 규약을 담고,
나머지 둘은 얇게 유지하는 구조.

### 리뷰 / 검증

**규칙 문서를 두 벌로 유지하자는 초안을 기각했다.**
`CLAUDE.md`와 `AGENTS.md`에 같은 내용을 각각 담으면 한쪽만 고쳐질 때 반드시 어긋난다.
그 시점에는 어느 쪽이 정본인지 알 수 없게 된다.
→ 정본은 `CLAUDE.md` 하나로 두고, `AGENTS.md`는 **포인터 + 자주 잘못 건드리는 것 3가지**만 남겼다.

**금지 항목을 명시적으로 넣도록 요구했다.**
규칙 문서는 결국 프롬프트라서, "이렇게 해라"보다 "이건 하지 마라"가 더 안전하게 작동한다.
특히 `rebase -i` / `squash` / `--amend` / `push --force`는 이 과제에서 **제출물을 파괴하는** 명령이다.
커밋 히스토리가 제출물의 일부인데 AI가 무심코 히스토리를 정리하면 되돌릴 수 없다.

**`moveQueue.ts`를 라이브러리로 대체하지 말라는 조항을 직접 추가했다.**
아직 존재하지 않는 파일이지만, 나중에 다른 세션의 AI가 코드를 보면
"TanStack Query 훅으로 단순화할 수 있다"고 제안할 것이 예상된다.
그 제안이 왜 틀렸는지(연속 이동 시 스냅샷이 이미 오염되어 있다는 것)를 **미리 근거와 함께** 적어두었다.
리팩터링 제안을 그때 가서 방어하는 것보다, 근거를 문서에 박아두는 편이 확실하다.

**계획 문서(`docs/PLAN.md`)는 그대로 싣지 않고 다시 썼다.**
초안은 평가 항목을 어떻게 공략할지의 관점으로 쓰여 있었고, 로컬 절대 경로와 개인 파일 참조가 섞여 있었다.
저장소에 남길 문서는 "어떻게 잘 보일까"가 아니라 "무엇을 왜 이렇게 정했나"를 적는 자리라고 판단해,
**요구사항 분해 / 기술 선택 근거 / 난이도가 걸린 지점 / 커밋 순서** 중심으로 재작성했다.

**검증하지 않은 것**: 실제로 새 세션을 열어 이 문서만 읽고 작업이 이어지는지는 아직 확인하지 못했다.
구현이 몇 단계 진행된 뒤 확인하고 결과를 여기 추가한다.

---

## [setup] 프로젝트 초기화

**검증 강도**: 경량 (구현 → 코드 정독)

### 프롬프트 1

> 커밋 1 `chore(setup)` 진행. Vite + React 19 + TS 초기화, 린트, 폴더 구조, README 뼈대.
> 의존성은 필요한 커밋에서 각각 추가할 것 — MSW는 커밋 2, TanStack Query는 커밋 7, react-virtual은 커밋 14.

### AI 출력 요지

`npm create vite`로 react-ts 템플릿 생성 후, Vitest·Testing Library·Prettier를 추가하고
`src/{features,components,mocks,types,lib,test}`로 폴더 구조를 정하고, `src/types/candidate.ts` 도메인 타입과 README 뼈대 작성.

### 리뷰 / 검증

**의존성을 한 번에 다 깔지 않기로 한 것은 의도적이다.**
초기 설정에서 필요한 패키지를 전부 설치하면 편하지만, 커밋 히스토리만 봐서는
어떤 라이브러리가 어떤 문제 때문에 들어왔는지 알 수 없다.
MSW는 mock API를 만들 때, TanStack Query는 낙관적 업데이트를 붙일 때 각각 들어오게 했다.

**`npm run build`가 실패했다.** `vite.config.ts`에서 `test` 키를 인식하지 못했다.

```
vite.config.ts(6,3): error TS2769: No overload matches this call.
  Object literal may only specify known properties, and 'test' does not exist in type 'UserConfigExport'
```

원인은 `defineConfig`를 `vite`에서 import한 것이었다. Vitest 4는 `vitest/config`의 `defineConfig`를 써야
`test` 키가 타입에 포함된다. AI 초안이 `vite`에서 가져오고 있었다.
→ import 출처를 `vitest/config`로 교체.

이어서 `/// <reference types="vitest/config" />`를 함께 넣었는데 린트가 잡았다.
`vitest/config`에서 직접 import하면 타입이 이미 잡히므로 삼중 슬래시 참조는 **불필요한 중복**이었다.
→ 삭제. 삭제 후 빌드·린트 모두 통과하는 것을 확인했다.

**`npm test`가 exit 1로 실패하는 것을 발견했다.** 아직 테스트 파일이 없어서다.
테스트는 커밋 10에서 들어오는데 그때까지 `npm test`가 빨간불이면
"원래 실패하는 것"으로 취급하게 되어 진짜 실패를 놓친다.
→ `--passWithNoTests`를 붙였다. 테스트가 생긴 뒤에도 동작이 달라지지 않으므로 그대로 둔다.

**`STAGES` 배열 순서에 주석으로 계약을 명시했다.**
이 배열은 단순한 목록이 아니라 **보드 컬럼 순서이자 이동 순서**를 동시에 정의한다.
나중에 누군가 알파벳순으로 정렬하면 화면과 이동 로직이 같이 깨진다. 주석 한 줄로 의도를 고정했다.

**스캐폴드 잔재를 정리했다.** Vite 템플릿의 `public/icons.svg`와 `src/App.css`, `src/assets/`는
기본 App.tsx를 교체하면서 참조가 사라졌다. 쓰이지 않는 파일이 저장소에 남으면
나중에 읽는 사람이 "어디선가 쓰이겠지"라고 생각하게 된다. → 삭제.
`public/favicon.svg`는 `index.html`이 참조하므로 남겼다.

**검증한 것**: `npm run typecheck` / `npm run build` / `npm run lint` / `npm test` 전부 통과.
**검증하지 않은 것**: 화면 렌더는 아직 제목뿐이라 브라우저 확인을 생략했다. 보드가 그려지는 커밋 3에서 확인한다.

---

## [mock-api] mock API — MSW 핸들러 · 저장소 · 시드

**검증 강도**: 중 (구현 → 실행 검증)

### 프롬프트 1

> 커밋 2 `feat(mock-api)` 진행. MSW 핸들러 + localStorage 백킹 + seed 1,000건 + 지연 200~800ms / 실패 15%.
> 요구사항상 이동이 mock API에 저장되어 새로고침 후에도 유지되어야 한다.
> 실패 동작을 확인하려면 15% 확률을 기다릴 게 아니라 강제할 수 있어야 한다.

### AI 출력 요지

`config.ts`(지연·실패율 + URL 쿼리 오버라이드) / `seed.ts`(1,000건 생성) / `db.ts`(localStorage 백킹) /
`handlers.ts`(GET 목록, PATCH 단계) / `browser.ts` · `server.ts` · `start.ts` 구성.

### 리뷰 / 검증

**시드를 `Math.random`으로 만든 초안을 고쳤다.**
새로고침할 때마다 이름과 지원일이 바뀌면 "아까 그 카드"를 다시 찾을 수 없다.
검증 중에 카드를 특정해야 하는 상황이 계속 생기는데 그때마다 대상이 달라진다.
→ 고정 시드 PRNG(mulberry32)로 교체했다. 재시드해도 항상 `c-0001 조하윤 / 백엔드 개발자`가 나온다.

**단계 분포를 균등에서 가중치로 바꿨다.**
초안은 5단계에 균등 분배해서 컬럼마다 200건씩 쌓였다. 실제 채용 파이프라인은 뒤로 갈수록 줄어들고,
무엇보다 **가상 스크롤이 필요한 상황(한 컬럼에 수백 건이 몰리는 것)이 드러나지 않는다.**
→ 서류검토 45 / 면접 22 / 처우협의 8 / 최종합격 10 / 불합격 15 가중치로 변경.
실제 생성 결과 457 / 198 / 74 / 112 / 159 로 의도대로 나왔다.

**늦게 도착한 응답을 시각으로 판별하려던 것을 `revision` 숫자로 바꿨다.**
초안은 `updatedAt` ISO 문자열을 비교해 최신 응답을 가리려 했다.
그런데 같은 밀리초 안에 두 번 갱신되면 두 응답의 `updatedAt`이 같아져서 순서를 가릴 수 없다.
빠른 연속 이동은 이 프로젝트에서 **일부러 만들어낼 상황**이라 실제로 걸린다.
→ 서버가 갱신할 때마다 1씩 올리는 `revision: number`를 추가했다.
`updatedAt`은 상세 화면 표시용으로 남겼다.

**`InvalidStage` 검증을 네트워크 시뮬레이션 앞으로 옮겼다.**
초안은 지연·실패를 먼저 태우고 body를 검증했다. 잘못된 요청은 서버에 닿기 전에 걸러지는 것이므로
800ms를 기다렸다가 400을 내는 건 실제 동작과 다르다. → 검증을 먼저, 지연은 그 뒤로.

**저장 실패를 조용히 넘기지 않게 했다.**
`writeStorage`의 `catch`를 초안은 빈 블록으로 뒀다. 용량 초과로 저장에 실패하면
화면은 정상으로 보이지만 새로고침하는 순간 변경이 사라진다. 원인을 찾기 어려운 종류의 실패다.
→ `console.warn`으로 남겼다.

**손상된 데이터와 스키마 버전 불일치를 처리했다.**
`JSON.parse`가 실패하면 앱 전체가 죽는다. 저장소에 뭐가 들어있을지는 통제할 수 없으므로
파싱 실패·버전 불일치·배열 아님을 모두 "다시 시드"로 처리했다.

**실행 검증** (Playwright, `npm run dev`):

| 확인 | 결과 |
|---|---|
| 목록 1,000건 조회 | 457 / 198 / 74 / 112 / 159 — 가중치대로 |
| PATCH 성공 | 200, `stage: offer`, `revision: 0 → 1` |
| localStorage 미러링 | 저장된 값도 `offer` / `revision 1` |
| **새로고침 후 유지** | 재조회 시 `offer` / `revision 1` 유지 |
| 실패 주입 `?failRate=1` | 503 `MOVE_FAILED`, **서버 상태 안 바뀜**(`revision 0` 유지) |
| 잘못된 단계 | 400 `INVALID_STAGE` |
| 지연 분포 | 279 / 802 / 463 / 694 / 381 / 800 ms |
| `?delay=0` 오버라이드 | 61ms (1,000건 직렬화 비용) |
| 손상된 localStorage | 1,000건으로 재시드, 스키마 버전 1 복구 |
| 콘솔 | 에러 3건 전부 위 테스트가 의도적으로 낸 503/400. 예기치 않은 에러 없음 |

지연이 802ms로 상한을 4ms 넘겼다. `delay()` 자체의 오차이며 요구 범위 해석상 문제 없다고 판단했다.

**검증 중 알게 된 것**: 실행 중에 localStorage를 지워도 목록이 그대로 나온다.
`db.ts`가 메모리를 1차 저장소로 쓰고 localStorage는 미러이기 때문이다.
실제 서버도 실행 중에 DB 파일을 지운다고 즉시 반영되지 않으므로 버그는 아니지만,
**저장소 관련 검증은 반드시 새로고침을 끼고 해야 한다**는 것을 확인했다.

**검증하지 않은 것**: 동시에 여러 PATCH를 보내는 상황은 확인하지 않았다.
경쟁 상태는 클라이언트 쪽 과제이므로 커밋 9에서 다룬다.

---

## [board-layout] 5단계 컬럼 레이아웃

**검증 강도**: 경량 (구현 → 코드 정독 → 실행 확인)

### 프롬프트 1

> 커밋 3 `feat(board-layout)`. 5단계 컬럼 레이아웃.
> 카드 컴포넌트는 커밋 4에서 만들 것이므로 지금은 이름만 찍는 자리표시자로 두고,
> 컬럼이 카드 렌더를 주입받는 구조로 만들 것.

### AI 출력 요지

`Board.tsx`(단계별 그룹핑 + 컬럼 배치) / `Column.tsx`(헤더 + 개수 + 본문) / `Board.module.css`.
카드는 `renderCard` prop으로 주입받아 다음 커밋에서 교체 가능하게 구성.

### 리뷰 / 검증

**단계별 분류를 `filter` 5회에서 1회 순회로 바꿨다.**
초안은 `STAGES.map(stage => candidates.filter(c => c.stage === stage))` 형태였다.
1,000건 × 5단계 = 5,000회 비교다. 지금은 체감되지 않지만 검색·필터가 붙으면
입력할 때마다 이 계산이 다시 돈다. 한 번 순회해 단계별 배열로 나누는 방식으로 교체했다.

**컬럼 폭을 고정에서 가변으로 바꿨다.**
초안은 `flex: 0 0 260px` 고정이었다. 1440px에서 확인해보니 5컬럼이 1,348px만 쓰고
오른쪽에 빈 공간이 남았다. `flex: 1 1 240px; min-width: 240px`로 바꿔
넓은 화면에서는 폭을 나눠 갖고, 좁아지면 240px에서 멈추고 보드가 가로 스크롤되게 했다.
하한이 없으면 좁은 화면에서 카드 내용이 읽을 수 없게 눌린다.

**`max-height: calc(100vh - 140px)` 매직넘버를 걷어냈다.**
140px은 헤더 높이를 눈대중으로 뺀 값이다. 헤더에 검색창(커밋 11)이 들어가면 바로 어긋난다.
셸이 이미 `display: flex; flex-direction: column; height: 100vh`이므로
보드에 `flex: 1; min-height: 0`을, 컬럼에 `max-height: 100%`를 줘서 부모 높이를 따라가게 했다.
`min-height: 0`이 필요한 이유는 flex 항목의 기본값이 `auto`라서, 없으면
내용 높이만큼 늘어나 스크롤이 생기지 않기 때문이다. 실제로 이 줄을 빼면 컬럼 스크롤이 사라진다.

**개수 표시에 `aria-label`을 붙였다.**
화면에는 `457`만 보이는데 스크린리더가 숫자만 읽으면 무엇의 개수인지 알 수 없다.
`aria-label="서류검토 457건"`으로 보충했다.
컬럼은 `section` + `aria-labelledby`로 랜드마크가 되어, 단계 5개를 순차 탐색하지 않고 바로 이동할 수 있다.

**실행 검증** (Playwright, 1440×900):

| 확인 | 결과 |
|---|---|
| 컬럼 5개 렌더 | ✓ |
| 표시 개수 = 실제 카드 수 | 457/198/74/112/159 전부 일치 |
| 보드 가로 스크롤 | 좁은 폭에서 동작 |
| 컬럼 세로 스크롤 | 5개 컬럼 모두 동작 |
| 랜드마크 | `region "서류검토"` ~ `region "불합격"` 5개 인식 |

**가상 스크롤 도입 전 기준값을 남겼다.** 커밋 14의 비교 대상이다.

| 항목 | 값 |
|---|---|
| 전체 DOM 노드 | 1,044 |
| 서류검토 컬럼 카드 노드 | 457 (표시 개수와 동일 = 전부 렌더됨) |

**검증하지 않은 것**: 키보드 탐색은 확인하지 않았다.
지금은 카드가 포커스 대상이 아니라 자리표시자 `div`라서 확인할 대상이 없다. 커밋 13에서 다룬다.

---

## [card-list] 지원자 카드

**검증 강도**: 경량 예정 → **codex 교차 리뷰로 승격**(실제 버그가 나와서)

### 프롬프트 1

> 커밋 4 `feat(card-list)`. 지원자 카드에 이름·직무·지원일·현재 단계를 표시.
> 현재 단계는 컬럼 위치로 이미 드러나므로 화면에 중복 표기할지 판단해서 알려줄 것.

### AI 출력 요지

`Card.tsx` + `Card.module.css` + `lib/date.ts`.
현재 단계는 화면에 중복 표기하지 않고 `sr-only` 텍스트로만 제공.
지원일은 카드에 "8월 1일", 스크린리더에는 연도 포함.

### 리뷰 / 검증

**`.sr-only` 클래스를 쓰면서 정의를 안 한 것을 잡았다.**
AI가 `className="sr-only"`를 썼는데 `index.css`에 그 클래스가 없었다.
빌드도 타입 체크도 통과한다. CSS 클래스 오타는 어디서도 걸리지 않는다.
증상은 **연도까지 화면에 그대로 노출되는 것**이라 "설계대로 안 보이네" 정도로 넘어갈 수 있었다.
→ `clip-path: inset(50%)` 기반으로 정의했다. `display: none`은 보조기술에서도 사라져서 쓸 수 없다.

**카드 높이를 고정했다.** 가변 높이면 커밋 14의 가상 스크롤에서 위치 계산이 어긋난다.
실측으로 50장 전부 74px 균일한 것을 확인했다.

### 교차 리뷰 (codex) — 이 커밋에서 처음 실행

경량 커밋이라 원래는 코드 정독으로 끝낼 계획이었으나,
**커밋 7(낙관적 업데이트)에서 codex를 처음 쓰다가 도구 문제로 시간을 버리는 것이 싫어서**
연결 확인을 겸해 이 커밋에 돌렸다. 결과적으로 실제 버그가 나왔다.

```
codex exec --sandbox read-only "Card.tsx와 date.ts를 읽고 접근성 관점에서
문제가 될 만한 점만 3개 이내로 지적해줘. 칭찬하지 말고 반박만.
각 지적에 재현 조건을 붙여줘."
```

**지적 ① `new Date('2026-08-01')`이 UTC로 파싱되어 시간대에 따라 하루 밀린다 — 채택**

재현 조건까지 줬길래 그대로 돌려봤다.

```
TZ=Asia/Seoul          2026-01-01 → 2026년 1월 1일
TZ=America/Los_Angeles 2026-01-01 → 2025년 12월 31일   ← 연도까지 틀림
                       2026-08-01 → 2026년 7월 31일
```

**codex가 말한 것보다 심했다.** 하루가 밀리는 정도가 아니라 연말·연초에는 연도가 틀린다.
`new Date()`가 날짜만 있는 문자열을 UTC 자정으로 파싱하는데 `Intl`은 로컬 시간대로 포매팅해서
UTC보다 뒤선 지역에서 어긋난다.

부끄러운 건, 내가 `date.ts` 주석에 **"화면에서 지워버리면 연말·연초에 어느 해인지 알 수 없어진다"**고
써놓고 정작 **연말·연초에 틀린 연도를 보여주는** 코드를 짰다는 점이다.
한국에서만 돌려봐서는 영원히 안 보였을 버그다.

→ `parseCalendarDate`로 연·월·일을 그대로 받아 로컬 날짜를 만들게 고쳤다.
지원일은 시각이 없는 달력상의 날짜라 애초에 시간대 변환의 대상이 아니다.
UTC+14(Kiritimati) ~ UTC-12(Etc/GMT+12) 네 개 시간대에서 재확인했다.

**지적 ② `<article>`에 접근성 이름이 없어 이름 없는 article이 반복된다 — 채택**

스크린리더의 요소 단위 탐색에서 이름 없는 article이 수백 개 나열되면,
안으로 들어가 본문을 읽어야만 누구인지 알 수 있다.
→ `aria-label={"이름 · 직무"}`를 붙였다. Playwright 로케이터가
`getByRole('article', { name: '권민준 · iOS 개발자' })`로 잡히는 것을 확인했다.

**지적 ③ `title` 속성에 의존하면 키보드·터치 사용자가 잘린 텍스트를 볼 수 없다 — 일부만 채택**

맞는 지적이지만 `title`을 없애면 마우스 사용자의 확인 수단까지 사라진다.
전체 정보는 **커밋 12의 상세 패널**에서 제공되고, 그 진입 경로는
커밋 13에서 키보드(Enter)로도 열리게 만든다.
→ `title`은 보조 수단으로 남기고, 잘린 텍스트의 전체 값은 상세 패널이 책임지는 것으로 정리했다.
접근성 이름(`aria-label`)에 이름과 직무를 모두 넣었으므로 스크린리더는 이미 전체 값을 읽는다.

**교차 리뷰 도구 자체의 문제**: `codex exec` 실행 시
`unknown variant 'max', expected one of none/minimal/low/medium/high/xhigh` 에러 로그가 함께 찍힌다.
CLI가 서버의 모델 목록 응답을 해석하지 못하는 버전 불일치다.
**리뷰 결과 자체는 정상적으로 나오므로** 그대로 쓰기로 했다.

### 실행 검증

| 확인 | 결과 |
|---|---|
| 이름·직무·지원일 렌더 | ✓ |
| `time[datetime]`과 표시 날짜 일치 | 3장 검사, 전부 일치 |
| `sr-only` 실제로 숨겨짐 | 1×1px로 축소 확인 |
| 카드 높이 균일 | 50장 전부 74px |
| 접근성 이름 | `한주원 · QA 엔지니어` 형태로 부여됨 |

**검증하지 않은 것**: 실제 스크린리더(VoiceOver)로 들어보지 않았다.
DOM 구조와 접근성 트리로만 확인했다.

---

## [loading-error-empty] 로딩 · 에러 · 빈 상태

**검증 강도**: 중 (구현 → 실행 검증)

### 프롬프트 1

> 커밋 5 `feat(loading-error-empty)`. 로딩·에러·빈 상태 세 가지를 UI로 처리.
> 빈 상태는 "데이터가 아예 없음"과 "이 컬럼에만 없음"이 다르다. 둘 다 다룰 것.
> 에러에는 재시도 경로를 줄 것.

### AI 출력 요지

`StateView`(제목·설명·재시도 버튼) / `BoardSkeleton`(컬럼 골격 + 셔머 카드) /
`App.tsx`를 `loading | error | ready` 유니온 상태로 재구성.

### 리뷰 / 검증

**상태를 불리언 세 개가 아니라 유니온으로 두게 했다.**
`isLoading` / `error` / `data`를 각각 두면 "로딩 중인데 에러도 있는" 조합이 타입상 표현 가능해지고,
화면에서 그 조합을 어떻게 그릴지 매번 판단해야 한다. 실제로 일어날 수 없는 상태는 만들 수 없게 하는 편이 낫다.

**로딩을 스피너가 아니라 컬럼 골격으로 만들었다.**
가운데 스피너 하나만 띄우면 로딩이 끝나는 순간 화면 구조가 통째로 바뀐다.
골격 카드 높이를 실제 카드와 같은 74px로 맞춰서 전환할 때 목록이 튀지 않게 했다.

**로딩 중 컬럼 개수를 감췄다.**
초안은 골격에도 `count={0}`을 그대로 넘겨 "0"이 보였다. 아직 모르는 값을 0으로 보여주면
"이 단계는 비었구나"라는 **오정보**가 된다. `loading` prop을 받아 숨기게 고쳤다.

**`prefers-reduced-motion`에서 셔머 애니메이션을 끈다.**
반복 애니메이션은 전정기관 장애가 있는 사용자에게 어지럼증을 유발한다.

**AI가 쓴 `display: contents` 꼼수를 걷어냈다.**
골격 카드를 `aria-hidden` 래퍼로 감싸면서 레이아웃이 깨지지 않게 `display: contents`를 썼는데,
카드마다 `aria-hidden`을 붙이면 래퍼 자체가 필요 없다. 우회할 문제를 만들고 우회한 셈이었다.

**`Column`에 없는 prop을 쓰고 있었다.** `BoardSkeleton`이 `loading`을 넘기는데 `Column`은 받지 않았다.
타입 체크에서 걸려서 바로 드러났다 — 이런 건 실행 전에 잡힌다.

**린트가 잡은 것에서 진짜 문제를 찾았다.**
`react(set-state-in-effect)` 경고가 떴다. 경고를 없애려고 구조를 보다가,
**재시도를 연달아 누르면 응답이 뒤섞이는** 문제가 있다는 걸 알았다.
mock 지연이 200~800ms로 들쭉날쭉해서 먼저 보낸 요청이 나중에 도착할 수 있고,
그러면 오래된 목록이 최신 목록을 덮어쓴다.
→ `AbortController`로 이전 요청을 취소하고, 취소가 늦더라도 `signal.aborted`를 확인해 반영을 막았다.
취소는 실패가 아니므로 에러 화면을 띄우지 않는 분기도 넣었다.

이 문제는 커밋 9(경쟁 상태)에서 다룰 주제와 같은 종류다. 목록 조회에도 이미 존재했다.

**에러 화면에서 같은 문장이 두 번 나오는 것을 실행 중에 발견했다.**

```
제목:  지원자 목록을 불러오지 못했습니다
설명:  지원자 목록을 불러오지 못했습니다.     ← mock API가 내려준 메시지
```

mock 핸들러의 `message`가 화면 제목과 같은 말을 하고 있었다.
→ **서버는 원인을, 화면은 무엇이 실패했는지를 말하도록** 분리했다.
서버 메시지를 `서버와 통신하지 못했습니다.`로 바꾸고,
화면이 `{원인} 잠시 후 다시 시도해 주세요.`로 조립한다.

**컬럼 높이가 제각각인 것을 스크린샷에서 발견했다.**
빈 컬럼을 확인하려고 2건만 남겨보니, 카드가 있는 컬럼만 길고 나머지는 짧았다.
단계마다 카드가 몇 장인지보다 **컬럼 크기 차이가 먼저 눈에 들어온다.**
→ `align-items: flex-start`를 `stretch`로 바꿔 보드 높이에 맞췄다. 실측 5개 컬럼 전부 819px.

**빈 상태 문구가 바로 위 헤더와 중복이었다.**
`면접 단계에 지원자가 없습니다`가 `면접` 헤더 바로 아래 붙으니 같은 단어가 두 번 보였다.
→ `지원자가 없습니다`로 줄였다. 컬럼이 `aria-labelledby`로 헤더와 묶여 있어 스크린리더 맥락은 유지된다.

### 실행 검증

| 확인 | 결과 |
|---|---|
| 로딩 골격 | 5컬럼 × 6장 = 30장, 개수 숨김, `aria-busy="true"` |
| 로딩 sr 안내 | "지원자 목록을 불러오는 중입니다." |
| 에러 (`?failRate=1`) | 제목·원인·재시도 버튼, 문장 중복 해소 확인 |
| 재시도 → 로딩 | 골격으로 전환됨 |
| 재시도 → 복구 | 실패율 0으로 바꾸고 재시도 시 1,000건 렌더, 에러 화면 사라짐 |
| 보드 전체 빈 상태 | "아직 지원자가 없습니다" + 안내 문구 |
| 컬럼 단위 빈 상태 | 서류검토 2건만 남겼을 때 나머지 4컬럼에 표시 |
| 컬럼 높이 | 819px 균일 |

**검증하지 않은 것**: 재시도 연타 시 응답이 뒤섞이지 않는 것은 코드로만 막았고 실제로 재현하지 않았다.
지연이 랜덤이라 손으로는 재현이 어렵다. 커밋 10에서 테스트로 확인한다.

### 커밋 직전에 사람이 잡은 결함 — 페이지가 통째로 스크롤됨

커밋 승인을 요청한 뒤, **카드가 끝난 지점 아래로 빈 화면이 한참 스크롤된다**는 지적을 받았다.
자동 검증도 내 실행 확인도 이걸 놓쳤다. 컬럼 스크롤은 정상이었고 카드도 다 보였기 때문이다.

측정해보니 원인이 분명했다.

```
window.innerHeight                    900
document.documentElement.scrollHeight  37538   ← 문서가 37,000px
document.body.scrollHeight             900
컬럼 본문 scrollHeight                 37486   ← 카드 457장의 실제 높이
```

컬럼 본문에 `overflow-y: auto`가 걸려 있어서 **카드는 시각적으로 잘렸지만**,
문서 전체 높이는 그대로 늘어나 있었다. 그래서 보드 아래로 빈 화면이 이어졌다.
보드가 화면을 꽉 채우고 있어서 **평소에는 그 빈 공간이 보이지 않다가, 스크롤해야 드러난다.**

**고치는 과정에서 두 번 틀렸다.**

1차 시도로 `body { overflow: hidden }`을 걸었다. 뷰포트로 전파되는 규칙에 기댄 것인데
문서는 여전히 37,538px이었고 스크롤도 막히지 않았다.
→ `html`에 직접 걸었다.

그 다음 `window.scrollTo(0, 5000)`으로 확인했는데 `scrollY`가 5000으로 나와서 "아직 안 고쳐졌다"고 판단했다.
**이 검증 자체가 틀렸다.** `overflow: hidden`은 사용자 스크롤을 막을 뿐,
`scrollTo`로 프로그래밍하는 스크롤은 그대로 동작한다.
→ 실제 휠 이벤트(`page.mouse.wheel`)로 다시 확인했다. 이번엔 `scrollY: 0`, 헤더도 제자리였다.

**틀린 검증으로 멀쩡한 코드를 계속 고칠 뻔했다.** 검증 방법이 검증 대상과 맞는지부터 봐야 했다.

`100vh` 대신 `height: 100%`를 쓴 것도 이때 함께 정리했다.
모바일 브라우저에서 `100vh`는 주소창이 접히기 전 높이를 가리켜 화면보다 커진다.

**이 시점에 "고쳤다"고 보고했는데, 사실 고친 게 아니었다.**

### 같은 결함의 진짜 원인 — 기기 모드에서 UI가 쪼그라듦

커밋 승인 전에 다시 지적을 받았다. 크롬 기기 모드에서 기종을 바꿀 때마다
UI가 축소되거나 여백이 사라지는 등 일관성 없이 깨진다는 것이었다.

처음에는 기기 모드를 켠 뒤 새로고침하지 않아 생기는 현상이라고 **틀리게 답했다.**
`browser_resize`로 375×667에서 재현을 시도했는데 정상이었기 때문이다.
그런데 크롬 기기 모드는 단순한 크기 변경이 아니라 `mobile: true` + DPR 변경까지 하는 에뮬레이션이라,
**뷰포트 크기만 바꾸는 방식으로는 재현되지 않는 조건이었다.**

CDP `Emulation.setDeviceMetricsOverride`로 동일하게 맞추자 바로 재현됐다.

```
iPhone 12 Pro   문서 37538px, 페이지 스크롤 가능
Pixel 9         문서 37538px, 페이지 스크롤 가능
```

**앞서 `html`에 걸었던 `overflow: hidden`은 스크롤을 막았을 뿐 문서 크기는 그대로였다.**
데스크톱에서는 그걸로 증상이 가려졌지만, 모바일 브라우저는 문서 크기에 맞춰
화면 전체를 축소(shrink-to-fit)하기 때문에 UI가 쪼그라들어 보인 것이다.
**증상을 덮었을 뿐 원인은 그대로 있었다.**

**두 번째 오진.** flex 항목의 `min-height: auto` 때문이라고 보고 `.column`에 `min-height: 0`을 넣었다.
`.board`에는 이미 넣었던 것이라 그럴듯했지만 문서 크기는 37,538px 그대로였다.

**추측을 멈추고 조상 체인을 전부 측정했다.**

| 요소 | clientHeight | scrollHeight | 넘침 |
|---|---:|---:|---:|
| `article.card` | 72 | 72 | 0 |
| `div.columnBody` | 1074 | 37486 | 36412 ← 스크롤 컨테이너, 정상 |
| `section.column` | 1117 | 1117 | **0** |
| `div.board` | 1127 | 1127 | **0** |
| `div.shell` | 1200 | 1200 | **0** |
| `div#root` | 1200 | 1200 | **0** |
| `body` | 1200 | 1200 | **0** |
| `html` | 1200 | **37538** | **36338** ← 여기서 되살아남 |

중간 요소는 전부 넘침 0인데 `html`에서만 스크롤 컨테이너 안쪽 콘텐츠 크기가 다시 나타났다.
**내 CSS의 레이아웃 문제가 아니라, 안쪽 스크롤 컨테이너의 콘텐츠 크기가 문서 크기로 새어나가는 경로가 있었던 것이다.**

→ `.shell`을 `position: fixed; inset: 0`으로 뷰포트에 고정했다.
고정 위치 요소는 문서의 스크롤 영역 계산에 포함되지 않으므로 그 경로 자체가 끊긴다.
`inset: 0`은 `100vh`와 달리 모바일 주소창이 접히고 펴져도 실제 보이는 영역을 따라간다는 이점도 있다.

**재확인 — 기기 에뮬레이션 5종 (CDP, `mobile: true`)**

| 기기 | 뷰포트 | 문서 | 페이지 스크롤 | 카드 높이 |
|---|---|---|---|---|
| iPhone SE | 375×667 | 375×667 | 없음 | 74 |
| iPhone 12 Pro | 390×844 | 390×844 | 없음 | 74 |
| iPhone 14 Pro Max | 430×932 | 430×932 | 없음 | 74 |
| Pixel 9 | 412×924 | 412×924 | 없음 | 74 |
| iPad Mini | 768×1024 | 768×1024 | 없음 | 74 |

**데스크톱 6종** (2560 / 1920 / 1707 / 1365 / 1093 / 800): 전부 문서 = 뷰포트, 페이지 스크롤 없음,
컬럼 높이 균일, 가로 스크롤은 1,093px 미만에서만 발생.

| 확인 | 결과 |
|---|---|
| 여백 위에서 휠 10회 | `scrollY: 0`, 헤더 `top: 16` 유지 |
| 컬럼 위에서 휠 6회 | 1번 컬럼만 1800px 스크롤, 2번 컬럼·페이지는 0 |
| 컬럼 끝까지 스크롤 | 마지막 카드 아래 여백 10px(패딩) — 빈 공간 없음 |

### 이 결함에서 배운 것

세 번 틀렸다. ① `body`에 `overflow`를 걸어 전파 규칙에 기댄 것,
② `scrollTo`로 검증해서 이미 고쳐진 것을 안 고쳐졌다고 판단한 것
(`overflow: hidden`은 프로그래밍 스크롤을 막지 않는다),
③ 기기 모드 문제를 재현 실패만으로 "환경 탓"이라고 답한 것.

공통점은 **원인을 확정하기 전에 고치려 든 것**이다.
조상 체인의 `clientHeight`/`scrollHeight`를 전부 찍어본 뒤에야 어디서 새는지가 한 번에 보였다.
그리고 **에뮬레이션 조건이 다르면 재현이 안 되는 결함이 있다** —
재현 실패를 "문제 없음"의 근거로 삼으면 안 된다.

---

## [stage-move] 단계 이동 + mock API 저장

**검증 강도**: 중 (구현 → 실행 검증)

### 프롬프트 1

> 커밋 6 `feat(stage-move)`. 액션 버튼으로 단계 이동 + PATCH 저장.
> 이동 규칙은 다음과 같이 가정한다: 정상 경로는 앞뒤 한 칸씩, 불합격은 어느 단계에서든 가능,
> 불합격에서의 복원은 서류검토로만.
> 낙관적 업데이트는 다음 커밋이므로 지금은 응답을 기다렸다가 반영할 것.

### AI 출력 요지

`stageRules.ts`(`nextStage` / `previousStage` / `canReject` / `canMove`),
`Card`에 액션 버튼 3개, `App`에 `handleMove` + `pendingId` 상태.

### 리뷰 / 검증

**이동 규칙을 데이터가 아니라 함수로 분리했다.**
"이동 가능한가"와 "다음은 어디인가"는 **화면(버튼 활성화)·키보드(커밋 13)·Undo(커밋 15)**
세 군데에서 필요하다. 각자 판단하게 두면 셋이 어긋난다.

**`canMove`에 Undo가 이 검사를 타면 안 된다는 주석을 달았다.**
`면접 → 불합격`을 되돌리면 면접으로 가야 하는데, 이 규칙으로는 건너뛰기라 막힌다.
되돌리기는 새 이동이 아니라 이미 있었던 상태로의 복원이므로 규칙 검사 대상이 아니다.
커밋 15에서 놓치기 쉬운 지점이라 규칙을 정의하는 자리에 미리 적어뒀다.

**버튼 이름에 대상 단계와 지원자 이름을 넣었다.**
AI 초안은 `aria-label="이전 단계로 이동"`이었다. 카드가 1,000개면
**같은 이름의 버튼이 3,000개** 생겨서 스크린리더로는 어느 카드의 버튼인지 알 수 없다.
→ `한주원을(를) 면접(으)로 이동` 형태로 바꿨다. 화면에도 `← 서류검토` / `처우협의 →`처럼
목적지를 적어서, 눌러보기 전에 어디로 가는지 알 수 있게 했다.

**카드 높이를 74px → 108px로 다시 고정했다.** 버튼 줄이 생겨 높이가 바뀌었다.
가변으로 두면 커밋 14의 가상 스크롤에서 위치 계산이 어긋난다.

**이동 중에는 버튼 3개를 모두 비활성화했다.**
지금은 응답을 기다려야 하므로 200~800ms 동안 아무 반응이 없다.
그 사이 연타하면 요청이 겹친다. 이 체감 지연과 잠금을 없애는 것이 다음 커밋의 목적이다.

**검증 중 내 테스트가 틀렸다 — persist 실패로 잘못 판단했다.**
이동 후 새로고침했더니 개수가 원래대로 돌아와서 "저장이 안 된다"고 봤다.
그런데 주소창에 `?reset=1`이 남아 있었고, 새로고침이 그 파라미터를 그대로 들고 가서
**설계대로 저장소를 초기화한 것**이었다. 파라미터를 빼고 다시 확인하니 정상이었다.
검증용으로 만든 장치가 검증을 오염시킨 경우다.

### 실행 검증

**이동 규칙 — 각 단계 첫 카드의 버튼 구성**

| 단계 | 버튼 |
|---|---|
| 서류검토 | `← 이전(비활성)` \| `면접 →` \| `불합격` |
| 면접 | `← 서류검토` \| `처우협의 →` \| `불합격` |
| 처우협의 | `← 면접` \| `최종합격 →` \| `불합격` |
| 최종합격 | `← 처우협의` \| `다음 →(비활성)` \| `불합격` |
| 불합격 | `← 서류검토` \| `다음 →(비활성)` \| `불합격(비활성)` |

**저장·실패**

| 확인 | 결과 |
|---|---|
| 이동 성공 | 면접 198 → 197, 처우협의 74 → 75 |
| **새로고침 후 유지** | 197 / 75 그대로 |
| 요청 중 | `data-pending="true"`, 불투명도 0.55, 버튼 3개 전부 비활성 |
| 실패(`?failRate=1`) | 개수 변화 없음, `role="alert"`로 `서버와 통신하지 못했습니다. 한주원님은 그대로 있습니다.` |
| 실패 후 | `data-pending="false"`로 해제되어 다시 시도 가능 |

### 커밋 직전 지적 — 넓은 화면에서 오른쪽·아래에 빈 공간

넓은 해상도에서 컬럼이 화면을 채우지 않고 오른쪽에 큰 여백이 남는다는 지적을 받았다.

내 환경에서는 재현되지 않았다. 1920에서 컬럼 폭 366, 2560에서 494로 정상적으로 늘어났고
여백은 패딩값(오른쪽 20px, 아래 16px)뿐이었다.

다만 **재현 실패를 "문제 없음"의 근거로 삼았다가 이미 한 번 틀린 적이 있어서**(기기 모드 건),
이번에는 원인 가능성 자체를 제거하는 쪽을 택했다.

컬럼이 늘어나지 않는 상태는 `.board`의 너비가 내용 기준으로 정해질 때 나타난다.
그러면 자식 컬럼이 `flex-basis: 240px`에 머문다.
`.board`는 부모의 `align-items: stretch`로 폭을 받고 있었는데, 이건 암묵적 의존이다.
→ `width: 100%`를 명시해 확정 너비로 만들었다.

**재확인 (8개 폭)**

| 뷰포트 | 컬럼 폭 | 오른쪽 | 아래 | 페이지 스크롤 |
|---|---:|---|---:|---|
| 2560 | 494 | 20px(패딩) | 16px | 없음 |
| 1920 | 366 | 20px | 16px | 없음 |
| 1600 | 302 | 20px | 16px | 없음 |
| 1280 | 240 | 가로 스크롤 | 16px | 없음 |
| 1024 / 900 / 700 / 375 | 240 | 가로 스크롤 | 16px | 없음 |

컬럼이 늘어나기 시작하는 경계는 약 1,300px이다(5 × 240 + 간격 + 패딩 = 1,288).

**검증하지 않은 것**: 키보드만으로 버튼에 도달하고 이동하는 경로는 확인하지 않았다.
버튼이라 Tab으로 닿기는 하지만, 카드가 1,000개면 Tab을 3,000번 눌러야 한다.
이 문제는 커밋 13에서 카드 단위 포커스와 방향키 조작으로 다룬다.

---

## [optimistic-update] 낙관적 업데이트 + 실패 롤백

**검증 강도**: 풀 (구현 → codex 교차 리뷰 → 실행 검증 → 사람 판단)

### 프롬프트 1

> 커밋 7 `feat(optimistic-update)`. TanStack Query 도입.
> 이동 시 UI를 먼저 반영하고, API 실패 시 원상 복구 + 사용자 피드백.
> 단, 공식 낙관적 업데이트 레시피의 전체 스냅샷 롤백은 쓰지 말 것.
> 같은 카드를 연속으로 옮기면 두 번째 onMutate가 이미 낙관 변경된 상태를 스냅샷으로 잡아서,
> 첫 번째가 실패하면 두 단계 전으로 되돌아간다. 되돌릴 대상을 필드 단위로 좁힐 것.

### AI 출력 요지

`queryClient.ts`(설정) / `api/candidates.ts` / `useMoveStage.ts`(mutation) /
`useToasts` + `ToastRegion`(피드백). `onMutate`에서 `{id, from}`만 컨텍스트로 반환하고
`onError`에서 그 카드의 `stage`만 되돌린다.

### 리뷰 / 검증

**전체 스냅샷 롤백을 쓰지 않기로 한 것은 지시에 넣었다.**
AI에게 맡겼으면 공식 레시피대로 `queryClient.getQueryData()` 스냅샷을 잡았을 것이다.
그 레시피는 **동시에 실행되는 mutation이 하나일 때만** 맞다.
이 화면은 지연 200~800ms에 실패 15%라 사용자가 응답을 기다리지 않고 계속 조작하는 것이 전제다.

```
1) 서류검토 → 면접    낙관 반영. 스냅샷 A = [서류검토]
2) 면접 → 처우협의    낙관 반영. 스냅샷 B = [면접]     ← 이미 낙관 변경된 상태를 잡는다
3) 1)이 실패          스냅샷 A로 복원 → 서류검토
```

두 번째가 성공했는데도 카드가 두 단계 전으로 간다. 게다가 스냅샷은 목록 전체라
그 사이 **다른 카드에 일어난 변경까지 같이 되돌린다**.

**`onSettled` 무효화를 빼기로 한 것은 내 판단이다.**
초안은 `onSettled`에서 목록을 무효화했다. 흔한 구성이지만 여기서는 두 가지가 걸린다.
이동 한 번마다 1,000건을 다시 받고, 그 응답이 도착하는 사이 사용자가 또 조작하면
**늦게 온 목록이 최신 낙관 상태를 덮어쓴다.**
서버가 이동 결과로 갱신된 카드를 그대로 돌려주므로 그 카드만 확정본으로 교체하면 된다.
`revision`·`updatedAt`까지 서버 값으로 맞춰진다.

**`mutations: { retry: 0 }`을 명시했다.**
단계 이동은 멱등이 아니다. 응답을 못 받았다고 재시도하면 서버에 두 번 적용될 수 있다.
실패는 사용자에게 알리고 판단을 맡긴다.

**실패 토스트에 "어디로 옮기려 했는지"와 "지금 어디 있는지"를 둘 다 넣었다.**
초안은 `이동에 실패했습니다`였다. 카드가 1,000개인 화면에서 그 문장만으로는
어느 카드가 어디로 돌아갔는지 직접 찾아야 한다.
→ `최건우님을 면접(으)로 옮기지 못했습니다. 서류검토에 그대로 있습니다.`

**성공은 `role="status"`, 실패는 `role="alert"`로 나눴다.**
`alert`는 스크린리더가 읽던 것을 끊고 즉시 알린다. 이동 성공은 끼어들 만한 일이 아니고,
실패는 끼어들어야 하는 일이다. 초안은 둘 다 `status`였다.

**죽은 코드를 정리했다.** 커밋 6의 `pending` 상태와 `moveError` 스타일은
낙관적 업데이트가 들어오면서 쓸 데가 없어졌다. 카드가 즉시 움직이므로 기다릴 대상이 없다.

**카드에 `data-id`를 추가했다.**
검증 중에 이름이 겹치는 지원자(`한주원`이 여러 명)를 화면에서 구분할 수 없어
**측정이 엉뚱한 카드를 가리키는 일이 있었다.** 포커스 복원(커밋 13)과
가상 스크롤(커밋 14)에서도 카드를 다시 찾아야 하므로 지금 넣었다.

### 실행 검증

| 확인 | 결과 |
|---|---|
| 낙관 반영 | 클릭 **60ms 후** 이미 다음 컬럼 (457→456, 198→199) |
| 성공 확정 | 서버 응답 후에도 동일, 성공 토스트 |
| **실패 롤백** | 낙관 반영(199→200) 후 정확히 **한 단계만** 복귀(200→199) |
| 롤백 후 서버 | 새로고침해도 원래 단계 — 서버도 안 바뀜 |
| 실패 토스트 | `최건우님을 면접(으)로 옮기지 못했습니다. 서류검토에 그대로 있습니다.` |

### 재현한 결함 — 늦게 도착한 실패가 확정된 성공을 덮어쓴다

훅 주석에 "남아 있는 문제"로 적어둔 것을 **실제로 재현했다.**
요청1을 실패+느리게(1500ms), 요청2를 성공+빠르게(100ms) 만들어 순서를 강제했다.

| 시점 | 화면 |
|---|---|
| 시작 | 서류검토 |
| 요청1 낙관 반영 | 면접 |
| 요청2 서버 확정 | 처우협의 |
| **요청1의 실패 도착** | **서류검토** ← 확정된 성공을 덮어씀 |
| 새로고침(서버 진실) | **처우협의** |

**화면은 서류검토, 서버는 처우협의.** 사용자는 성공한 이동이 되돌아간 것을 보고,
새로고침하면 또 달라진다. 필드 단위 롤백은 "두 단계 전으로 가는" 문제는 없앴지만
**같은 카드에 요청이 겹치는 것 자체**는 막지 못한다.

이 과제의 주제가 여기서 그대로 드러난다. **가장 위험한 실패는 에러가 나는 것이 아니라
"됐다"고 말하는 것**인데, 낙관적 업데이트는 그 상태를 UI가 의도적으로 만들어내는 기능이다.
단순히 롤백을 붙이는 것만으로는 부족하고, **어느 응답이 최신인지를 판단하는 층**이 따로 필요하다.

다음 두 커밋에서 다룬다 — `fix`로 늦게 온 실패를 무시하게 하고, 그 다음 직렬 큐로 겹침 자체를 없앤다.

---

## [optimistic-update] 늦게 도착한 응답이 확정 상태를 덮어쓰는 문제 수정

**검증 강도**: 풀 (직전 커밋에서 재현한 결함 수정 → 실행 검증 + 회귀 확인)

### 프롬프트 1

> 직전 커밋에서 재현한 결함을 고쳐줘.
> 요청1(실패, 느림)과 요청2(성공, 빠름)가 같은 카드에 겹치면
> 요청1의 실패가 뒤늦게 도착해 요청2가 확정한 상태를 덮어쓴다.
> 화면은 서류검토, 서버는 처우협의로 어긋난다.
> 서버 revision을 쓸지 클라이언트 순번을 쓸지 판단하고 근거를 알려줘.

### AI 출력 요지

카드별 요청 순번(`seq`)을 `useRef`의 Map에 두고, `onMutate`에서 발행,
`onError`/`onSuccess`에서 그 카드의 최신 순번과 같을 때만 캐시에 반영.

### 리뷰 / 검증

**서버 `revision` 대신 클라이언트 순번을 쓴 판단이 맞는지 따져봤다.**
`revision`은 서버가 갱신에 성공할 때만 올라간다.
**실패한 요청은 revision을 올리지 않으므로**, 서버 값만으로는 "실패한 옛 요청"을 구분할 수 없다.
지금 문제가 되는 것이 정확히 그 실패한 옛 요청이라 서버 값으로는 풀리지 않는다.
→ 클라이언트 순번이 맞다. `revision`은 성공 응답의 확정본을 반영할 때 그대로 쓰인다.

**순번을 `useState`가 아니라 `useRef`에 뒀다.**
순번이 바뀌었다고 다시 그릴 이유가 없다. `useState`면 이동할 때마다 1,000개 카드가 다시 렌더된다.

**지나간 요청의 실패 토스트를 띄우지 않기로 했다.**
처음에는 롤백만 막고 토스트는 그대로 띄우려 했다. 그런데 문구가
`한주원님을 면접(으)로 옮기지 못했습니다. 서류검토에 그대로 있습니다.`인데
카드는 실제로 처우협의에 있다. **틀린 정보를 알리는 것보다 알리지 않는 편이 낫다.**

사용자 입장에서 봐도 그렇다. 면접을 눌렀다가 처우협의를 눌렀고 결과는 처우협의다.
원하던 곳에 도착했는데 중간 단계의 실패를 알릴 이유가 없다.
성공 토스트는 이미 떴으므로 피드백이 없는 것도 아니다.

**`onSuccess`에도 같은 검사를 넣었다.**
초안은 `onError`에만 넣었다. 성공 응답도 지나간 것일 수 있다 —
요청1이 늦게 성공하면 요청2가 만든 최신 낙관 상태를 되돌린다. 같은 문제다.

### 실행 검증

**수정 전에 재현했던 시나리오 그대로**

| 시점 | 수정 전 | 수정 후 |
|---|---|---|
| 시작 | 서류검토 | 서류검토 |
| 요청1 낙관 반영 | 면접 | 면접 |
| 요청2 서버 확정 | 처우협의 | 처우협의 |
| 요청1의 실패 도착 | **서류검토** ← 덮어씀 | **처우협의** ← 무시됨 |
| 새로고침(서버) | 처우협의 | 처우협의 |
| **화면 = 서버** | **어긋남** | **일치** |

**회귀 확인**

| 확인 | 결과 |
|---|---|
| 단일 실패 롤백 | 서류검토 → (낙관)면접 → 서류검토, 에러 토스트 정상 |
| 같은 카드 3연타(전부 성공) | 화면·서버 모두 최종합격 — 마지막 조작과 일치 |

**남은 한계 (커밋 9에서 다룸)**: 이건 잘못된 반영을 막을 뿐 요청이 겹치는 것 자체를 없애지 않는다.
겹치는 동안 롤백 기준점 `from`이 서버 확정값이 아니라 낙관 상태일 수 있어서,
연속 이동이 **모두** 실패하면 되돌릴 지점이 정확하지 않다.
직렬 큐로 겹침 자체를 없애면 `from`이 항상 직전 확정 상태가 된다.

---

## [mock-api] 조회와 이동의 실패율 분리

**검증 강도**: 중 (사람이 지적 → 수정 → 실행 검증)

### 배경 — 사람이 지적한 것

커밋 8 확인을 요청하면서 `?failRate=1`에서 이동을 눌러보라고 안내했는데,
**그 화면에는 카드가 없다**는 지적을 받았다.

`failRate`가 목록 조회에도 적용되어 목록을 못 받아오고 에러 화면만 나온다.
확인하려는 것은 "이동이 실패했을 때 카드가 제자리로 돌아오는가"인데,
그러려면 카드가 먼저 보여야 한다.

내 검증에서는 `failRate=0`으로 띄운 뒤 `history.replaceState`로 주소를 바꿔치기했다.
자동화로는 되지만 **사람이 쓸 수 있는 형태가 아니다.**
검증 장치를 만들어놓고 나만 쓸 수 있게 만든 셈이다.

### 프롬프트 1

> `?failRate=1`이면 목록 조회도 실패해서 카드가 안 보인다.
> 이동만 실패시키는 방법이 필요하다. 조회와 이동의 실패율을 따로 지정할 수 있게 해줘.

### AI 출력 요지

`readMockConfig(operation)`이 작업 종류를 받고,
`moveFailRate` / `fetchFailRate`가 있으면 그것을, 없으면 공통 `failRate`를, 그것도 없으면 기본값을 쓴다.

### 리뷰 / 검증

**기존 `failRate`를 없애지 않고 폴백으로 남겼다.**
`failRate`는 "둘 다"를 뜻하는 값으로 그대로 두고, 작업별 값이 있으면 그것이 이긴다.
이미 `failRate`로 적어둔 문서와 커밋 메시지가 있어서, 의미가 바뀌면 그것들이 전부 틀린 설명이 된다.

| 파라미터 | 목록 조회 | 이동 |
|---|---|---|
| `?failRate=1` | 실패 | 실패 |
| `?moveFailRate=1` | 정상 | 실패 |
| `?fetchFailRate=1` | 실패 | 정상 |

### 실행 검증

| URL | 결과 |
|---|---|
| `?moveFailRate=1` | 카드 1,000건 로드 → 이동 시 낙관 반영 후 롤백, 에러 토스트 |
| `?fetchFailRate=1` | 카드 0건, 에러 화면 |
| `?failRate=1` | 카드 0건, 에러 화면 (기존 동작 유지) |

**검증 중 내 판단이 한 번 틀렸다.** 처음 확인에서 `fetchFailRate=1`이 에러 화면을 안 띄우길래
문제가 있는 줄 알았는데, 쿼리에 `retry: 1`이 걸려 있어 재시도까지 끝나는 데 시간이 더 걸리는 것이었다.
대기를 4초로 늘리니 정상이었다. **기다리는 시간이 부족한 것과 동작하지 않는 것은 다르다.**

---

## [race-condition] 카드별 직렬 큐 + 확정 단계 기준 롤백

**검증 강도**: 풀 예정이었으나 **codex 교차 리뷰 실패** (아래 기록) → 실행 검증으로 대체

### 프롬프트 1

> 커밋 9 `feat(race-condition)`. 같은 카드의 이동 요청이 겹치는 것 자체를 없애야 한다.
> 카드별 직렬 큐로 순서를 보장하되 다른 카드끼리는 서로 기다리지 않게 할 것.
> 그리고 겹친 요청이 **모두** 실패하는 경우를 확인해줘.
> 두 번째 요청의 "직전 단계"는 첫 번째가 만든 낙관 값이라, 그걸로 되돌리면
> 서버에 한 번도 저장된 적 없는 단계가 화면에 남는다.
> React에 의존하지 않는 모듈로 분리할 것 — 다음 커밋에서 테스트해야 한다.

### AI 출력 요지

`moveQueue.ts`에 `createMoveCoordinator()`. 카드별 순번·진행 중 요청 수·
서버 확정 단계·직렬 실행 꼬리를 관리하고, `begin` / `isLatest` /
`settleSuccess` / `settleFailure` / `enqueue`를 제공.

### 리뷰 / 검증

**세 겹으로 나뉘는 문제라는 것을 명시했다.** 하나로 뭉뚱그리면 어느 것이 무엇을 막는지 흐려진다.

| 겹 | 막는 것 |
|---|---|
| 직렬 큐 | 서버 도착 순서가 사용자 클릭 순서와 달라지는 것 |
| 순번(seq) | 큐를 통과해도 응답 처리 시점에 이미 지나간 요청이 화면을 되돌리는 것 |
| 확정 단계 기준 롤백 | 서버에 저장된 적 없는 단계로 되돌아가는 것 |

**"확정 단계"를 언제 갱신할지가 핵심이었다.**
초안은 매 요청의 `candidate.stage`를 되돌림 기준으로 삼았다. 그건 낙관 값이다.
→ **진행 중인 요청이 없을 때의 값만** 서버 확정 상태로 신뢰하게 했다(`inflight === 0`).
겹친 요청은 모두 첫 번째가 잡은 확정값을 공유한다.

**지나간 성공 응답도 확정값은 갱신한다.**
`settleSuccess`는 `stale`을 돌려주더라도 `confirmedByCard`는 갱신한다.
화면에 반영하지 않을 뿐, 서버가 확정한 사실 자체는 유효하다.
이걸 빠뜨리면 나중 요청이 실패했을 때 더 옛날 단계로 되돌린다.

**큐에서 앞선 요청이 실패해도 다음 요청을 실행한다.** (`previous.then(task, task)`)
사용자가 이미 누른 조작이고, 앞이 실패했다고 뒤를 취소하면 조용히 무시하는 셈이 된다.

**큐 꼬리를 지울 때 자기가 꼬리인지 확인한다.**
그 사이 새 요청이 붙었으면 그쪽이 꼬리다. 무조건 지우면 직렬화가 끊긴다.

**실패 토스트가 틀린 위치를 알릴 수 있는 것을 발견했다.**
`App.tsx`가 `candidate.stage`(클릭 시점 값)로 "어디에 있는지"를 표시하고 있었다.
요청이 겹치면 실제 되돌린 단계와 다르다.
→ `onError` 콜백에 `restoredTo`를 넘겨 **실제로 되돌린 단계**를 쓰게 했다.
이 과제 내내 반복된 문제와 같은 종류다 — 화면이 실제와 다른 것을 말하는 것.

### 교차 리뷰 실패 — 기록

이 커밋은 계획상 codex 교차 리뷰를 포함하는 풀 검증 구간이었다. **실패했다.**

| 시도 | 결과 |
|---|---|
| 1차 (`useMoveStage.ts`, 상세 맥락 포함) | 18분간 출력 없음, 중단 |
| 2차 (범위를 좁히고 짧은 프롬프트) | 5분 초과, 중단 |

커밋 4에서는 같은 도구가 2분 만에 응답했고 실제 버그(시간대 파싱)를 찾아냈다.
그 실행에서도 `unknown variant 'max'` 버전 불일치 오류가 함께 찍혔는데,
지금은 응답 자체가 오지 않는다.

작업 규칙에 적어둔 **"같은 시도 3회 실패 시 중단하고 사람에게 보고"**에 따라
세 번째 시도를 하지 않고 멈췄다. 시간을 더 쓰는 대신 실행 검증으로 대체했다.

**대체 수단이 더 약한 근거는 아니라고 본다.** 교차 리뷰는 "깨질 만한 곳을 지목"하는 것이고,
아래 검증은 **깨지는 시나리오를 실제로 만들어 결과를 확인**한 것이다.
다만 "내가 생각하지 못한 각도"를 놓쳤을 가능성은 남아 있고, 그건 이 방식으로 메울 수 없다.

### 실행 검증

**A. 연속 이동이 모두 실패** — 확정 단계 기준 롤백이 없으면 깨지는 경우

| 시점 | 결과 |
|---|---|
| 시작 | 서류검토 |
| 면접으로 이동 시도 (실패) | — |
| 처우협의로 이동 시도 (실패) | — |
| 최종 | **서류검토** — 낙관 값(면접)으로 가지 않음 |
| 토스트 | `한주원님을 처우협의(으)로 옮기지 못했습니다. 서류검토에 그대로 있습니다.` |

**B. 같은 카드 3연타 (전부 성공)** — 직렬 큐가 순서를 지키는가

네트워크 요청을 가로채 실제 전송 순서를 확인했다.

```
PATCH 순서: interview → offer → hired      (클릭 순서와 동일)
화면: 최종합격    서버(새로고침): 최종합격    일치
```

**검증하지 않은 것**: 서로 다른 카드가 동시에 이동할 때 큐가 서로를 막지 않는지는
코드로만 보장했고 실행으로 확인하지 않았다. 다음 커밋의 테스트에서 다룬다.

---

## [test] 롤백 · 경쟁 상태 · 큐 순서 테스트

**검증 강도**: 풀 (테스트 작성 → **테스트 자체를 검증** → 실행 확인)

### 프롬프트 1

> 커밋 10 `test(optimistic-rollback)`. 낙관적 업데이트 롤백과 경쟁 상태를 테스트로 고정할 것.
> `moveQueue.ts`는 순수 모듈이니 순서를 직접 지정해 검증하고,
> 훅은 MSW로 캐시 반영까지 확인할 것. 앱의 mock은 15% 확률로 실패하니 그대로 쓰지 말 것.

### AI 출력 요지

`moveQueue.test.ts` 9개(직렬 큐 3 / 지나간 응답 판별 2 / 롤백 기준점 4),
`useMoveStage.test.tsx` 6개(낙관 반영 2 / 실패 롤백 2 / 겹친 요청 2).

### 리뷰 / 검증

**테스트마다 핸들러를 직접 지정하게 했다.**
앱의 mock은 15% 확률로 실패한다. 그대로 쓰면 테스트가 무작위로 깨진다.
`onUnhandledRequest: 'error'`도 켜서, 의도하지 않은 요청이 조용히 통과하지 않게 했다.

**응답 시점을 직접 통제하는 `deferred()` 헬퍼를 썼다.**
직렬 큐를 확인하려면 "앞 작업이 끝나기 전에 뒤 작업이 시작하지 않는다"를 봐야 하는데,
타이머로는 그 순간을 잡을 수 없다. 앞 작업을 붙잡아둔 채 시작 순서를 관찰한다.

### 테스트가 실제로 버그를 잡는지 확인했다

**통과하는 테스트는 그 자체로는 아무것도 증명하지 않는다.**
구현을 일부러 망가뜨려 각 테스트가 실패하는지 확인했다.

| 고장낸 것 | 실패한 테스트 |
|---|---|
| 확정 단계 대신 낙관 값을 롤백 기준으로 | 3개 (롤백 기준점 2, 겹친 요청 1) |
| 지나간 응답 판별 제거 | 2개 |
| 직렬 큐 제거(즉시 실행) | 1개 |

**여기서 테스트의 빈틈을 하나 찾았다.**

"지나간 응답 판별"을 제거했을 때, 처음에는 통합 테스트가 **통과했다**.
직렬 큐가 생긴 뒤로는 요청1이 끝난 뒤에 요청2가 시작하므로
**최종 상태만 보면** seq 검사 없이도 결과가 같기 때문이다.

그러나 그 사이 화면은 `offer → screening → offer`로 튄다.
사용자에게는 "성공한 이동이 되돌아갔다가 다시 돌아오는" 것으로 보인다.
최종 값만 확인하는 테스트는 이걸 못 잡는다.

→ 캐시 변경을 구독해 **거쳐 간 단계 전부**를 기록하고,
`offer`에 도달한 뒤로 `screening`이 다시 나타나지 않는지 확인하는 테스트를 추가했다.
이 테스트는 seq 검사를 제거하면 실패한다(확인함).

**최종 상태만 보는 테스트는 "잠깐 잘못된 화면"을 놓친다.**
낙관적 업데이트는 중간 상태를 사용자에게 그대로 보여주는 기능이라, 중간 상태도 검증 대상이다.

### 결과

```
moveQueue.test.ts      10개   직렬 큐 / 지나간 응답 판별 / 롤백 기준점
useMoveStage.test.tsx   6개   낙관 반영 / 실패 롤백 / 겹친 요청
합계 16개 통과
```

**검증하지 않은 것**: 컴포넌트 렌더링 테스트(버튼 비활성화, 토스트 표시)는 작성하지 않았다.
이번 커밋의 목표는 눈으로 확인하기 어려운 순서·롤백 로직을 고정하는 것이고,
화면 표시는 실행 검증으로 매 커밋 확인해 왔다.

---

## [search-filter] 이름 검색 + 직무 필터

**검증 강도**: 중 (구현 → 실행 검증 + 성능 실측)

### 프롬프트 1

> 커밋 11 `feat(search-filter)`. 이름 검색 + 직무별 필터.
> 요구사항은 "200건 이상이어도 체감 지연 없이"인데 지금 데이터는 1,000건이다.
> debounce와 useDeferredValue 중 무엇을 쓸지 판단하고 근거를 알려줄 것.
> 그리고 개선 전후를 수치로 남길 수 있게 해줘.

### AI 출력 요지

`filterCandidates.ts`(순수 필터) + `FilterBar.tsx` + `App`에서 `useDeferredValue` 적용.
입력창은 즉시 값을, 목록은 지연된 값을 쓴다.

### 리뷰 / 검증

**debounce 대신 `useDeferredValue`를 골랐다.**
debounce는 "몇 밀리초를 기다릴지"를 정해야 한다. 그 숫자는 기기 성능과 데이터 양에 따라
적정값이 달라지는데, 근거 없이 300ms 같은 값을 박아두게 된다.
`useDeferredValue`는 대기 시간을 정하지 않는다 — 입력 반영을 먼저 처리하고,
한가해지면 목록을 갱신한다. 느린 기기에서는 더 미루고 빠른 기기에서는 바로 처리한다.

**직무 목록을 전체 기준으로 만들게 했다.**
초안은 걸러진 결과에서 직무를 뽑았다. 그러면 `iOS 개발자`를 고른 순간
선택지에 `iOS 개발자`만 남아서 다른 직무로 바꿀 수 없다.

**이름 검색에 공백 제거와 NFC 정규화를 넣었다.**
공백은 "홍 길동"으로 쳐도 찾게 하려는 것이고, NFC 정규화는 macOS 때문이다.
macOS에서 한글을 입력하거나 파일명·다른 앱에서 붙여넣으면 자모가 분리된 NFD로 들어오는 경우가 있다.
**눈으로는 똑같은 "김승현"인데 코드포인트가 달라 검색이 안 된다.**
실제로 NFD 문자열을 넣어 4건이 나오는 것을 확인했다.

**빈 상태를 두 종류로 나눴다.**
"아직 지원자가 없습니다"와 "조건에 맞는 지원자가 없습니다"는 다르다.
후자는 조건을 바꾸면 해결되는 상태라 **필터 해제 버튼**을 함께 뒀다.

**결과 건수를 `aria-live`로 알린다.**
시각적으로는 목록이 줄어드는 것이 보이지만, 스크린리더 사용자는 입력 후 몇 건인지 알 방법이 없다.
목록을 직접 훑어야 한다.

### 성능 실측 — `useDeferredValue`를 빼고 재봤다

"최적화했다"는 말을 근거 없이 쓰지 않기 위해, 구현을 되돌려 같은 조건에서 측정했다.
1,000건 기준, `김` → `김승` → `김승현` → 지우기 순서로 입력하며
입력 이벤트부터 다음 프레임까지의 시간과 롱태스크(50ms 초과)를 기록했다.

| | 최대 입력 지연 | 롱태스크 |
|---|---:|---|
| `useDeferredValue` 없음 | **156ms** | 155ms, 81ms |
| `useDeferredValue` 적용 | **58ms** | 58ms |

가장 비싼 구간은 검색어를 **지울 때**다(4장 → 1,000장 마운트). 156ms → 33ms.

58ms 롱태스크는 남아 있다. 1,000장을 한 번에 마운트/언마운트하는 비용이라
가상 스크롤(커밋 14)에서 다시 측정한다.

### 실행 검증

| 확인 | 결과 |
|---|---|
| 직무 필터 (`iOS 개발자`) | 113명, 컬럼 49/25/6/14/19 |
| 직무 + 이름 조합 | 8명 |
| 결과 없음 | 0명 + "조건에 맞는 지원자가 없습니다" + 필터 해제 버튼 |
| 필터 해제 | 1,000명 복귀, 컬럼 457/198/74/112/159 |
| 공백 무시 (`김 승 현`) | 4명 |
| **NFD 입력** | 4명 — 정규화 없으면 0명 |
| **필터 상태에서 이동** | 정상 동작, 카드가 다음 단계로 이동 |

**검증하지 않은 것**: 필터가 걸린 상태에서 이동한 카드가 필터 조건에서 벗어나면
목록에서 사라지는데, 그때 포커스가 어디로 가는지는 확인하지 않았다. 커밋 13에서 다룬다.

---

## [detail-panel] 사이드 패널 상세

**검증 강도**: 중 (구현 → 실행 검증)

### 프롬프트 1

> 커밋 12 `feat(detail-panel)`. 카드 클릭 시 사이드 패널로 상세 표시.
> 모달이므로 포커스 트랩, Esc 닫기, 닫을 때 포커스 복원까지 포함할 것.
> 패널에서도 단계 이동이 가능해야 하고, 이동하면 패널 내용도 따라 바뀌어야 한다.

### AI 출력 요지

`DetailPanel.tsx` + `useFocusTrap.ts`. `role="dialog"` / `aria-modal` /
배경 클릭·Esc·닫기 버튼으로 닫기. 카드 요약부를 버튼으로 바꿔 상세 진입점으로 사용.

### 리뷰 / 검증

**카드 요약부를 `div` + `onClick`이 아니라 `button`으로 만들게 했다.**
초안은 `article`에 `onClick`을 달았다. 마우스로는 되지만 **키보드로는 열 수 없고**,
스크린리더에도 "누를 수 있는 것"으로 보이지 않는다.
카드 안에 이미 이동 버튼 3개가 있어서 카드 전체를 버튼으로 만들 수는 없으므로,
이름·직무·지원일 묶음만 버튼으로 감쌌다.

**선택된 지원자를 객체가 아니라 id로 들고 있게 했다.**
초안은 `useState<Candidate|null>`에 카드 객체를 담았다. 그러면 패널에서 단계를 옮겼을 때
**패널은 옛 단계를 계속 보여준다.** id로 들고 캐시에서 다시 찾으면 자동으로 따라간다.

그리고 찾을 때 **필터된 목록이 아니라 전체 목록에서** 찾는다.
필터를 바꿔 그 카드가 목록에서 빠져도 열어둔 패널이 갑자기 닫히지 않게 하기 위함이다.

**패널을 열 때 첫 요소가 아니라 패널 자체에 포커스를 준다.**
첫 버튼으로 바로 보내면 스크린리더가 **패널 제목을 읽지 못하고** 그 버튼부터 읽는다.
어떤 창이 열렸는지 모른 채 조작하게 된다.

**`onEscape`를 `useCallback`으로 감쌌다.**
매 렌더 새 함수면 `useEffect`가 계속 재실행되고, 그때마다 포커스가 패널로 되돌아간다.
패널 안에서 Tab을 눌러도 제자리로 튕긴다.

### 실행 검증에서 찾은 결함 — 이동 후 포커스 소실

| 확인 | 결과 |
|---|---|
| Enter로 열기 | `role="dialog"`, `aria-modal="true"`, 포커스가 패널에 |
| Tab 8회 | 전부 패널 안에서 순환 (닫기 → 이메일 → 전화 → 이전 → 불합격 → 닫기 …) |
| 패널에서 이동 | 패널의 "현재 단계"가 면접으로 갱신됨 |
| Esc / 닫기 버튼 / 배경 클릭 | 모두 닫힘 |
| Esc 후 포커스 | 열었던 카드 버튼으로 복원 |
| **이동 후 Esc** | **포커스가 `BODY`로 떨어짐** ← 결함 |

패널에서 카드를 이동시키면 그 카드는 다른 컬럼에 다시 그려지고 **원래 버튼은 언마운트된다.**
`previouslyFocused.isConnected`가 false라 복원이 건너뛰어지고 포커스가 문서 맨 위로 간다.
키보드 사용자는 카드 하나를 옮길 때마다 **보고 있던 자리를 잃는다.**

→ `useFocusTrap`에 `resolveFallback`을 추가했다.
원래 위치가 사라졌으면 같은 `data-id`를 가진 새 카드를 찾아 그쪽에 포커스를 준다.
패널을 닫는 렌더가 끝난 뒤 찾아야 새로 그려진 요소를 잡을 수 있어서 `requestAnimationFrame`을 썼다.

**재확인**: 이동 후 Esc → 포커스가 **면접 컬럼의 같은 카드** 버튼으로 이동.
이동 없이 닫는 경우도 그대로 동작(회귀 확인).

**검증하지 않은 것**: 스크린리더로 실제 읽어보지 않았다. 접근성 트리와 포커스 이동으로만 확인했다.

---

## [a11y-keyboard] 카드 단위 포커스 + 방향키 조작

**검증 강도**: 풀 (구현 → **codex 교차 리뷰 성공** → 실행 검증)

### 프롬프트 1

> 커밋 13 `feat(a11y-keyboard)`. 키보드만으로 단계 이동과 상세 열람이 가능해야 한다.
> 지금은 카드 하나에 포커스 가능한 요소가 4개라 1,000건이면 Tab을 4,000번 눌러야 한다.
> 도달 가능한 구조로 바꾸고, 키 배치를 정한 근거를 알려줄 것.

### AI 출력 요지

`useBoardKeyboard.ts`. roving tabindex(컬럼당 카드 하나만 Tab 대상),
`↑↓` 카드 이동 / `←→` 단계 이동 / `Home·End` / `Enter·Space` 상세 / `Delete` 불합격.

### 리뷰 / 검증

**`←→`를 포커스 이동이 아니라 단계 이동에 배정했다.**
목록 위젯의 관례로는 `←→`가 컬럼 간 포커스 이동이다. 하지만 이 화면에서 사용자가 반복하는 일은
**카드를 옮기는 것**이지 컬럼을 구경하는 것이 아니다. 컬럼 간 이동은 Tab으로 이미 된다.

**컬럼별로 마지막 포커스 카드를 기억하게 했다.**
초안은 항상 첫 카드를 Tab 대상으로 삼았다. 그러면 컬럼을 Tab으로 떠났다 돌아올 때마다
**맨 위로 튕겨서** 300번째 카드를 보고 있었어도 처음부터 다시 내려와야 한다.

**카드 안 버튼들을 Tab 순서에서 뺐다(`tabIndex={-1}`).**
카드를 Tab 대상으로 만들었는데 안쪽 버튼도 그대로 두면 Tab 횟수가 줄지 않는다.
버튼의 기능은 전부 키로 제공되고(`←→`, `Enter`, `Delete`), 마우스로는 그대로 눌린다.

**막힌 방향을 눌렀을 때 안내를 띄운다.**
최종합격에서 `→`를 누르면 아무 일도 안 일어난다. **키가 안 먹는 건지 끝인 건지 알 수 없다.**
`조하윤님은 더 진행할 단계가 없습니다.`로 알린다.

**키보드 조작 안내를 화면에 표시했다.**
방향키로 단계를 옮길 수 있다는 것은 화면을 봐서는 알 수 없다.
**안내가 없으면 만들어둔 기능이 없는 것과 같다.**

### 교차 리뷰 (codex) — 이번엔 성공했다

커밋 9에서 두 번 실패했던 도구가 이번에는 응답했다. 지적 2개 중 **1개는 내가 놓친 것**이었다.

**지적 ① 카드당 Tab 정지점 4개, 1,000건이면 4,000번 — 이미 수정 중이던 것**
같은 문제를 독립적으로 지목했다. roving tabindex를 대안으로 제시한 것도 같았다.

**지적 ② 이동 후 포커스 복원 장치가 없다 — 놓치고 있었다**

> 이동 버튼을 누르면 카드가 다른 컬럼으로 이동하면서 현재 포커스된 버튼이 언마운트될 가능성이 크다.
> 포커스가 `body`로 떨어지면 사용자가 방금 옮긴 카드나 주변 카드로 복귀하기 어렵다.

커밋 12에서 **상세 패널의** 포커스 복원은 고쳤는데, **보드에서 방향키로 옮길 때**는 같은 문제가
그대로 남아 있었다. 한쪽을 고치고 같은 종류의 다른 쪽을 못 본 것이다.

재현해보니 정확했다.

```
이동 전  포커스: c-0004 (서류검토)
→ 누름
이동 후  포커스: BODY        카드는 면접 컬럼에 있음
```

**키보드 사용자는 카드 하나를 옮길 때마다 보드 처음부터 다시 찾아야 한다.**

**고치는 과정에서 한 번 틀렸다.** `requestAnimationFrame`으로 다음 프레임에 찾게 했는데
여전히 포커스를 잃었다. 낙관적 반영이 React의 렌더 커밋 뒤에 일어나서,
다음 프레임에 찾으면 아직 옛 위치의 노드를 잡거나 이미 사라진 노드를 잡는다.
→ 옮길 카드 id를 ref에 담아두고, **목록이 실제로 다시 그려진 뒤**(`useEffect`) 찾게 바꿨다.

### 실행 검증

| 확인 | 결과 |
|---|---|
| **Tab 횟수** | 5번으로 5개 컬럼 전부 도달 (기존 4,000번) |
| `↑` `↓` | 같은 컬럼 안에서 카드 이동 |
| `End` | 컬럼 마지막 카드로 |
| `→` `→` `→` `←` 연속 | 면접 → 처우협의 → 최종합격 → 처우협의, **매번 포커스가 카드를 따라감** |
| `Delete` | 불합격 컬럼으로 이동, 포커스 유지 |
| 막힌 방향(`최종합격`에서 `→`) | `조하윤님은 더 진행할 단계가 없습니다.` |
| **마우스 없이 전체 시나리오** | 검색창 → Tab ×2 → 카드 → Enter → 상세 → Esc → 카드 복귀 |

**검증하지 않은 것**: 실제 스크린리더(VoiceOver·NVDA)로 들어보지 않았다.
접근성 트리·포커스 이동·`aria-live` DOM으로만 확인했다.
`role="status"`가 실제로 방해 없이 읽히는지, `role="alert"`가 끼어드는지는 확인 못 했다.

---

## [virtualization] 컬럼 가상 스크롤

**검증 강도**: 풀 (구현 → 실행 검증 → A/B 실측 → 회귀 확인)

### 프롬프트 1

> 커밋 14 `feat(virtualization)`. 1,000건에서 컬럼 목록을 가상화할 것.
> **커밋 13의 키보드 이동과 충돌한다.** `↑↓`와 `Home/End`가
> `column.querySelectorAll('article')`로 형제를 찾는데, 화면 밖 카드는 DOM에 없다.
> 그 충돌을 어떻게 풀지 먼저 설명하고 구현할 것. 전후 수치를 남길 수 있게 해줘.

### AI 출력 요지

`VirtualCardList.tsx`(`@tanstack/react-virtual`), 행 높이 116px(카드 108 + 간격 8),
overscan 6. 키보드는 `data-index`/`data-count`로 **순번 기반**으로 전환.

### 리뷰 · 검증

**키보드 이동을 "DOM 형제 탐색"에서 "순번 계산"으로 바꿨다.**
기존 방식은 그려진 카드 목록에서 다음 항목을 골랐다. 가상화하면
`End`가 진짜 마지막 카드가 아니라 **"지금 그려져 있는 마지막 카드"**로 간다.
가상 목록이 남긴 `data-index`(진짜 순번)와 `data-count`(전체 개수)로 목표 순번을 계산하고,
그 카드가 없으면 그 위치로 스크롤해 그려지게 한 뒤 포커스를 준다.

**overscan을 6으로 뒀다.** 화면 바로 밖의 카드가 미리 그려져 있으면
`↑↓` 한 칸 이동에서 스크롤을 기다릴 필요가 없다. 실제로 ↓ 30회 연속이
스크롤 대기 없이 순번 1→30으로 이어졌다.

### 세 번 틀렸고, 세 번째에 원인을 찾았다

**1차 — `End`가 동작하지 않음.** 스크롤 후 `requestAnimationFrame` 한 번으로 찾았다.
가상 목록은 스크롤 이벤트를 받아 다시 렌더해야 그 행이 생긴다. 한 프레임으로는 부족했다.
→ 몇 프레임 동안 확인하는 방식으로 바꿨다. **그래도 실패했다.**

**2차 — 단계 이동 후 포커스 소실이 재발.** 커밋 13에서 고친 것이 다시 깨졌다.
옮겨간 카드가 **대상 컬럼의 화면 밖 위치**면 DOM에 없어서 `querySelector`가 못 찾는다.
→ 카드가 어느 단계의 몇 번째인지 알려주는 `locate`를 호출부에서 받아,
그 위치로 스크롤한 뒤 포커스하게 했다. **이것도 실패했다.**

**3차 — 원인을 측정했다.** 추측을 멈추고 스크롤 컨테이너의 실제 값을 찍었다.

```
count: 457,  rowHeight: 116   →  기대 scrollHeight ≈ 53,012
실제 scrollHeight: 1,510
최대 scrollTop:      714
프레임별 렌더 범위:  0~19 (15프레임 내내 변화 없음)
```

**스크롤이 714px에서 멈춰 있었다.** 아무리 기다려도 456번째 행은 그려질 수 없었다.

원인은 `.columnBody`를 그대로 재사용한 것이었다. 그 클래스는 `display: flex`인데,
가상 목록은 **전체 높이를 가진 스페이서 div** 하나를 두고 그 안에 행을 절대 배치한다.
flex 컨테이너 안에서 그 스페이서가 **flex 항목으로 축소**되어,
53,012px로 지정한 높이가 1,510px이 됐다.

→ 가상 스크롤 전용 클래스(`.virtualBody`, `display: block`)를 따로 만들었다.
고친 뒤 `scrollHeight`가 53,032로 기대값과 일치했고, 앞의 두 문제가 함께 해결됐다.

**앞의 두 수정이 헛수고는 아니었지만, 원인을 확정하기 전에 두 번이나 고치려 들었다.**
스크롤이 되지 않는다는 사실 하나만 먼저 확인했으면 한 번에 끝났을 일이다.

### A/B 실측

커밋 3에 남긴 기준값(DOM 1,044)은 쓸 수 없었다. 그때 카드는 이름만 있는 자리표시자 1노드였고,
지금은 버튼 3개를 포함해 카드 하나가 약 13노드다. **같은 조건이 아니다.**
그래서 현재 구현에서 가상 스크롤만 되돌려 다시 측정했다.

| 1,000건 기준 | 가상 스크롤 없음 | 적용 | |
|---|---:|---:|---|
| 전체 DOM 노드 | **13,081** | **996** | 13분의 1 |
| 렌더된 카드 | 1,000 | 65 | 컬럼당 13 |
| 서류검토 컬럼 | 457 | 13 | |
| 검색 최대 입력 지연 | 147ms | **44ms** | |
| 롱태스크(50ms 초과) | 147ms 1회 | **없음** | |

커밋 11에서 `useDeferredValue`로 156ms → 58ms까지 줄였던 것이,
가상 스크롤로 **롱태스크 자체가 사라졌다.**

### 회귀 확인 (키보드)

| 확인 | 결과 |
|---|---|
| `End` | 457개 중 **456번(마지막)** — 그려진 마지막이 아님 |
| `Home` | 0번 |
| `↓` 30회 연속 | 순번 1→30 끊김 없음 |
| 단계 이동 후 포커스 | screening → interview → offer, 매번 카드를 따라감 |
| 시각 확인 | 카드 간격·스크롤바·조작 안내 정상 |

**검증하지 않은 것**: 카드 높이를 116px로 하드코딩했다. 이름이나 직무가 길어져
카드가 두 줄이 되면 위치 계산이 어긋난다. 지금 데이터에서는 한 줄로 잘리게 되어 있어 문제가 없지만,
동적 높이 측정(`measureElement`)은 적용하지 않았다. `DECISIONS.md`에 한계로 적는다.

---

## [undo] 되돌리기

**검증 강도**: 중 (구현 → 실행 검증)

### 프롬프트 1

> 커밋 15 `feat(undo)`. 방금 한 이동 취소.
> **`stageRules.canMove` 검사를 타면 안 된다.** 면접 → 불합격을 되돌리면
> 불합격 → 면접인데 규칙상 한 칸 건너뛰기라 막힌다.
> 되돌리기도 mock API를 거치므로 실패할 수 있다. 별도 경로를 만들지 말 것.

### AI 출력 요지

`useUndo.ts`(직전 이동 1건 기억) + 성공 토스트의 "되돌리기" 버튼 + `Cmd/Ctrl+Z`.
되돌리기는 `move.mutate`를 그대로 타서 낙관적 업데이트·롤백을 공유한다.

### 리뷰 / 검증

**규칙 우회를 커밋 6에 미리 적어둔 것이 여기서 값을 했다.**
`stageRules.ts`의 `canMove` 주석에 "Undo는 이 검사를 타지 않는다"를 적어뒀다.
아홉 커밋이 지난 뒤에 구현하면서 그 제약을 다시 떠올릴 필요가 없었다.
실제로 규칙은 UI 버튼의 활성화에만 쓰이고 mutation 경로에는 없어서, 별도 처리 없이 동작한다.

**되돌리기가 다시 "되돌릴 수 있는 이동"으로 기록되던 문제를 검증에서 찾았다.**
`Cmd+Z`를 두 번 누르면 원래대로 돌아왔다. **토글이 된 것이다.**

```
면접으로 이동 → Cmd+Z → 서류검토 → Cmd+Z → 면접  (되돌리기의 되돌리기)
```

요구사항은 "방금 한 이동을 취소"라는 **한 단계**다.
→ `MoveInput`에 `isUndo`를 두고, 되돌리기로 실행된 이동은 기록하지 않게 했다.
지금은 두 번째 `Cmd+Z`가 `되돌릴 이동이 없습니다.`로 끝난다.

**스택이 아니라 1건만 기억한다.**
여러 단계를 쌓으면, 그 사이 다른 카드를 옮겼거나 같은 카드가 다시 움직였을 때
"이 시점으로 되돌린다"는 것이 무엇을 뜻하는지 애매해진다.
요구사항이 "방금 한 이동"이므로 확실하게 되돌릴 수 있는 범위만 제공한다.

**기억해둔 뒤 카드가 또 움직였으면 되돌리지 않는다.**
`take(current)`가 현재 카드의 단계를 확인한다. 기록 시점의 목적지에 그대로 있지 않으면
(다른 조작이나 실패 롤백으로 움직였다면) 되돌리기는 의미가 없고,
그때 되돌리면 **사용자가 의도하지 않은 곳으로 보내게 된다.**

**입력창에서는 `Cmd+Z`를 가로채지 않는다.**
검색어를 지우려고 누른 `Cmd+Z`가 카드를 되돌려버리면 예측할 수 없는 도구가 된다.
`input, select, textarea` 안에서는 브라우저의 텍스트 실행 취소가 우선이다.

**되돌리기도 낙관적 업데이트와 롤백을 그대로 탄다.**
별도 경로를 만들지 않았으므로 15% 실패, 직렬 큐, 확정 단계 기준 롤백이 모두 적용된다.

### 실행 검증

| 확인 | 결과 |
|---|---|
| **면접 → 불합격 → Undo** | **면접으로 복귀** — 규칙상 막히는 경로인데 되돌아감 |
| 새로고침 | 면접 유지 (서버에도 반영) |
| `Cmd+Z` | 동작 |
| **`Cmd+Z` 두 번** | 두 번째는 `되돌릴 이동이 없습니다.` — 토글되지 않음 |
| 되돌리기 실패(`moveFailRate=1`) | 면접에 그대로, `서류검토(으)로 옮기지 못했습니다. 면접에 그대로 있습니다.` |
| 입력창에서 `Cmd+Z` | 검색어가 지워짐(텍스트 실행 취소), 카드는 그대로 |

**검증하지 않은 것**: 되돌리기 대상 카드가 화면 밖(가상 스크롤)에 있을 때
되돌린 뒤 그 카드로 스크롤·포커스가 이동하지는 않는다.
토스트 버튼으로 되돌리는 경우 포커스는 토스트에 있고, 카드는 조용히 제자리로 간다.
개선 여지로 `DECISIONS.md`에 적는다.
