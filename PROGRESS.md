# 진행 상태

> 새 세션은 [`CLAUDE.md`](./CLAUDE.md) → 이 파일 순서로 읽는다.
> **이어서 작업할 때 쓸 프롬프트**: `CLAUDE.md와 PROGRESS.md를 읽고, "지금 하던 것"부터 이어서 진행해줘. 커밋은 하지 말고.`

**최종 갱신**: 커밋 11 `feat(search-filter)` 준비 완료 (미커밋)

---

## 커밋 체크리스트

- [x] 0  `docs(rules)`            CLAUDE.md · AGENTS.md · PROGRESS.md · docs/PLAN.md   ← **지금 여기**
- [x] 1  `chore(setup)`           Vite + React + TS, 린트, 폴더 구조, README 뼈대
- [x] 2  `feat(mock-api)`         MSW + localStorage persist + seed 1,000건 + 지연/실패
- [x] 3  `feat(board-layout)`     5단계 컬럼 레이아웃
- [x] 4  `feat(card-list)`        지원자 카드
- [x] 5  `feat(loading-error-empty)`  로딩 / 에러+재시도 / 빈 상태
- [x] 6  `feat(stage-move)`       액션 버튼 이동 + PATCH persist
- [x] 7  `feat(optimistic-update)`  낙관적 반영 + 실패 롤백 + 피드백
- [x] 8  `fix(optimistic-update)`   연속 이동 시 롤백이 두 단계 전으로 가던 문제
- [x] 9  `feat(race-condition)`   카드별 직렬 큐 + seq 기반 stale 응답 폐기
- [x] 10 `test(optimistic-rollback)`  롤백·경쟁 상태·큐 순서 테스트
- [x] 11 `feat(search-filter)`    이름 검색 + 직무 필터
- [ ] 12 `feat(detail-panel)`     사이드 패널 상세
- [ ] 13 `feat(a11y-keyboard)`    키보드 내비게이션 + aria-live + 포커스 관리
- [ ] 14 `feat(virtualization)`   컬럼 가상 스크롤 (1,000건)
- [ ] 15 `feat(undo)`             되돌리기
- [ ] 16 `docs(decisions)`        DECISIONS.md 정리
- [ ] 17 `docs(readme)`           실행법·스택·성능 수치·배포 링크

---

## 지금 하던 것

커밋 11 `feat(search-filter)` 완료, **커밋 승인 대기 중**.

`filterCandidates.ts`(순수) + `FilterBar.tsx` + `useDeferredValue`.
debounce 대신 `useDeferredValue`를 쓴 이유: 대기 시간을 숫자로 못 박지 않아도 된다.
이름 검색에 공백 제거 + NFC 정규화(macOS NFD 입력 대응).

성능 실측: 구현을 되돌려 A/B 비교. 최대 입력 지연 156ms → 58ms, 필터 해제 시 156ms → 33ms.

다음: 커밋 12 `feat(detail-panel)` — 사이드 패널 상세

---

## 열린 이슈 / 확인 필요

_(막힌 것·미해결 버그·사람 확인이 필요한 것을 여기 적는다. 3회 실패로 중단한 시도도 여기 남긴다.)_

- 규칙 문서만 읽고 새 세션이 작업을 이어갈 수 있는지 미검증 — 구현이 몇 단계 진행된 뒤 실제로 확인할 것
- `npm test`에 `--passWithNoTests`가 붙어 있다. 커밋 10에서 실제 테스트가 들어온 뒤,
  테스트 파일이 사라져도 통과해버리는 문제가 없는지 확인할 것
- 반응형은 375px까지 확인했다. 그 폭에서는 컬럼이 1.4개만 보이고 나머지는 가로 스크롤이다
  (칸반 보드의 표준 동작). 좁은 화면에서 단계 탭으로 한 컬럼씩 보여주는 방식은
  Must/Should를 끝낸 뒤 여유가 있으면 검토할 것
- 실제 스크린리더(VoiceOver)로 들어본 적 없다. DOM 구조와 접근성 트리로만 확인 중.
  커밋 13에서 한 번은 실제로 들어볼 것
- `codex exec` 실행 시 `unknown variant 'max'` 에러 로그가 함께 찍힌다(CLI-서버 버전 불일치).
  리뷰 결과 자체는 정상이라 그대로 쓰는 중
- **codex 교차 리뷰가 동작하지 않는다.** 커밋 4에서는 2분 만에 응답하며 실제 버그를 찾았는데,
  커밋 9에서는 18분 무응답 + 재시도 5분 초과로 두 번 실패했다.
  남은 풀 검증 구간(커밋 13 a11y, 14 가상 스크롤)에서 한 번 더 시도해보고,
  계속 실패하면 그 사실을 DECISIONS.md에 한계로 명시할 것
- mock API는 여전히 어떤 단계로든 이동을 허용한다. 규칙은 클라이언트에만 있다.
  실제 서버라면 서버가 검증해야 하지만, 이 과제에서는 UI 동작이 평가 대상이라 클라이언트에 뒀다.
  `DECISIONS.md`에 적을 것

---

## 확정된 가정

> `DECISIONS.md`와 동기화한다. 모호한 요구사항을 스스로 가정하고 명시하는 것도 평가 대상이다.

| # | 가정 | 근거 |
|---|---|---|
| 1 | 검색·필터는 클라이언트에서 처리한다 | mock API가 전체 목록을 내려주는 구조이고, 1,000건은 클라이언트가 감당 가능한 규모다. 서버 페이지네이션까지 흉내내면 낙관적 업데이트·경쟁 상태라는 이 과제의 핵심에서 초점이 흐려진다 |
| 2 | 잘못된 요청(존재하지 않는 단계)은 지연·실패 시뮬레이션 대상이 아니다 | 실제 서버에서도 body 검증은 네트워크에 닿기 전 단계다 |
| 3 | 정상 경로(서류검토~최종합격)는 앞뒤 한 칸씩만 이동한다 | 실제 채용 프로세스와 가깝고, ←/→ 키 조작과 자연스럽게 맞는다. 이동 불가 상태가 생겨야 버튼 비활성화와 그때의 포커스 거동을 검증할 수 있다 |
| 4 | 불합격은 어느 단계에서든 가능하고, 복원은 서류검토로만 | 탈락은 어느 시점에서든 일어난다. 복원 지점을 여러 개 두면 "어느 단계에서 탈락했는지"를 서버가 기억해야 하는데, 그건 이 과제 범위 밖이다 |
| 5 | Undo는 이동 규칙 검사를 타지 않는다 | 되돌리기는 새 이동이 아니라 이미 있었던 상태로의 복원이다. 규칙으로 막으면 되돌릴 수 없는 이동이 생긴다(면접 → 불합격 → Undo) |

검토 예정 항목:
- 최종합격 / 불합격에서 이전 단계로 되돌리기를 허용할 것인가
- 단계 건너뛰기(서류검토 → 처우협의)를 허용할 것인가
- 최종합격과 불합격을 병렬 종료 단계로 볼 것인가
- 검색·필터를 클라이언트에서 처리할 것인가 서버(mock)로 넘길 것인가

---

## 측정 기록

> "개선했다"는 말 대신 수치를 남긴다. README에 옮겨 적을 원본.

| 항목 | 이전 | 이후 | 측정 방법 |
|---|---|---|---|
| 1,000건 컬럼 DOM 노드 수 | — | — | 가상 스크롤 도입 전/후 |
| 검색 입력 지연 | — | — | 1,000건 기준 |
| mock 지연 실측 | — | 279~802ms | 커밋 2, GET 6회 |
| 검색 입력 최대 지연 (1,000건) | 156ms | **58ms** | 커밋 11, useDeferredValue A/B |
| 검색어 지울 때 (4→1,000장) | 156ms | **33ms** | 커밋 11 |
| 롱태스크(50ms 초과) | 155ms·81ms | 58ms 1회 | 커밋 11 |
| 전체 DOM 노드 (1,000건) | 1,044 | — | 커밋 3 기준값. 커밋 14에서 비교 |
| 서류검토 컬럼 카드 노드 | 457 | — | 표시 개수와 동일 = 전부 렌더됨 |
| 단계 분포 (1,000건) | — | 457/198/74/112/159 | 서류검토/면접/처우협의/최종합격/불합격 |
