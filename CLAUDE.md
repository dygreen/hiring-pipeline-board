# CLAUDE.md — 이 저장소에서 AI가 지켜야 할 규칙

> **이 파일이 정본이다.** 세션·도구(Claude Code / Codex CLI / Cursor)가 바뀌어도 규칙은 이 문서 하나를 따른다.
> 작업을 시작하기 전에 **이 파일 → `PROGRESS.md` 순서로 읽는다.**

---

## 1. 이 저장소는 무엇인가

프론트엔드 **채용 과제** 제출물이다. 주제는 **채용 파이프라인 보드**(서류검토 → 면접 → 처우협의 → 최종합격/불합격)이며, 지원자 카드를 단계 간 이동시켜 관리한다. 실제 백엔드는 없고 mock API만 쓴다.

### 이 저장소가 중요하게 여기는 것

**결과물만큼 결과에 이른 과정이 남아야 한다.**

| 무엇이 남는가                                           | 어디에         |
| ------------------------------------------------------- | -------------- |
| 요구사항을 어떤 기능 단위로 쪼갰는지                    | 커밋 히스토리  |
| AI에 무엇을 어떻게 지시했고 그 출력을 어떻게 검증했는지 | `PROMPTS.md`   |
| 어떤 설계를 왜 골랐고 무엇을 포기했는지                 | `DECISIONS.md` |

코드가 동작하는 것만으로는 작업이 끝나지 않는다. **기록이 함께 남아야 완료다.**
빨리 끝내려고 기록을 뒤로 미루지 않는다. 미룬 기록은 정확하지 않게 쓰이거나 아예 쓰이지 않는다.

### 작성자가 결과물에 책임을 진다

AI가 작성한 코드라도 저장소에 들어가는 순간 작성자의 코드다.
**설명할 수 없는 코드는 저장소에 들어가지 않는다.** (§4)

### 이 저장소에서 특히 조심하는 것

- **커밋을 몰아서 하는 것** — 어떤 판단이 어떤 순서로 이뤄졌는지 추적할 수 없게 된다.
  시행착오로 되돌린 흔적도 그 과정의 일부이므로 지우지 않는다.
- **검증하지 않은 것을 검증했다고 적는 것** — 기록 하나가 거짓이면 기록 전체를 믿을 수 없다.
- **미사용 코드·죽은 코드를 방치하는 것, 같은 문제를 저장소 안에서 서로 다른 방식으로 푸는 것** —
  읽는 사람이 어느 쪽이 의도된 방식인지 알 수 없게 된다.
- **필수 요구사항 미충족** — 특히 **낙관적 업데이트의 실패 롤백**은 이 과제의 핵심 동작이다.

---

## 2. 절대 금지 (하지 말 것)

- **커밋하지 말 것.** 파일만 만들고 무엇이 바뀌었는지 보고한다. **커밋과 푸시는 사람이 판단한다.** (§6 참조)
- `git rebase -i` / `squash` / `commit --amend` / `push --force` / `reset --hard` **금지**.
  → **커밋 히스토리는 제출물의 일부다.** 시행착오 커밋(고쳤다 되돌린 흔적)도 그 과정의 기록이므로 지우지 않는다.
- **여러 기능을 한 커밋에 섞지 말 것.** 원칙은 `기능 하나 = 커밋 하나 = 기록 한 섹션`.
- **`PROMPTS.md`의 `리뷰/검증` 항목을 상상해서 쓰지 말 것.**
  실제로 실행·재현·리뷰한 내용만 적는다. 검증하지 않았으면 **"검증하지 않음"이라고 적는다.**
  꾸며낸 검증 기록 하나가 나머지 기록 전체를 못 믿게 만든다.
- **미사용 코드·죽은 코드·주석 처리된 코드를 남기지 말 것.**
- **요청 범위 밖의 파일을 고치지 말 것.**
- **의존성을 임의로 추가하지 말 것.** §5 스택표에 없는 패키지는 사람에게 먼저 묻는다.
- `git add -A` 금지. 확인한 파일만 명시적으로 스테이징한다.

---

## 3. 모호하면 추측 금지, 역질문으로 해소

LLM은 모른다고 말하기보다 **그럴듯하게 채우는 경향**이 있다. 그래서 명시한다.

- 요구사항이 모호하면 **추측해서 진행하지 말고 묻는다.**
- 단, **질문은 한 번에 묶어서** 한다. 한 단계씩 물으면 사람이 계속 붙어 있어야 한다.
- 물어볼 만큼은 아니지만 판단이 필요했던 것은 **가정으로 확정하고 `DECISIONS.md`에 명시**한다.
  과제 9장: _"요구사항이 모호하면 스스로 가정하고 그 가정을 DECISIONS.md에 명시(가정하는 능력도 평가 대상)."_

## 3-1. 막혔을 때의 상한 (무한 루프 방지)

- 같은 시도가 **3회 실패하면 중단**하고 사람에게 보고한다.
- 검증이 **3회 연속 실패하면 중단**한다. 코드를 계속 흔들지 않는다.
- 중단할 때는 **무엇을 시도했고 무엇이 막혔는지**를 `PROGRESS.md`의 「열린 이슈」에 남긴다.

---

## 4. 기능 완료의 정의 (DoD)

아래 **5개가 전부 충족되어야** 커밋 후보가 된다. 하나라도 빠지면 완료가 아니다.

1. **코드가 동작한다** — `npx tsc --noEmit` 에러 0, 관련 테스트 통과
2. **작성자가 코드를 읽고 설명할 수 있다** — 왜 이렇게 짰는지, 이 줄이 없으면 무엇이 깨지는지.
   설명할 수 없는 부분이 있으면 **그 자리에서 묻고 이해한 뒤에** 넘어간다. 나중에 보겠다고 미루지 않는다.
3. **`PROMPTS.md`에 해당 섹션이 추가되었다** — 프롬프트 원문 + AI 출력 요지 + **실제** 리뷰/검증
4. **§7 검증 강도표에 맞는 검증을 수행했다**
5. **`PROGRESS.md`가 갱신되었다**

---

## 5. 아키텍처 규칙

### 스택 (이 목록 밖의 것을 임의로 추가하지 말 것)

| 항목            | 선택                                                  |
| --------------- | ----------------------------------------------------- |
| 빌드/프레임워크 | Vite + React 19 + TypeScript                          |
| 서버 상태       | TanStack Query v5                                     |
| 클라이언트 상태 | `useState` / `useReducer` (전역 스토어 도입하지 않음) |
| mock API        | **MSW** — 지연 200~800ms, 실패 약 15%                 |
| 가상 스크롤     | `@tanstack/react-virtual`                             |
| 테스트          | Vitest + Testing Library + MSW/node                   |
| 스타일          | CSS Modules                                           |

### 폴더 구조

```
src/
├── features/board/     보드·컬럼·카드·이동 로직  ← 핵심
├── features/detail/    상세 패널
├── components/         공용 UI (Toast, Skeleton, EmptyState 등)
├── mocks/              MSW 핸들러·seed·localStorage 백킹 저장소
├── types/              도메인 타입
└── lib/                순수 유틸
```

### 넘지 말아야 할 경계 — TanStack Query가 하는 일 vs 직접 짜는 일

과제 4장: _"핵심 상태 로직을 통째로 대신해 주는 라이브러리에 의존한 경우, 그 선택 이유를 문서에 밝힐 것."_
따라서 **조정(coordination) 로직은 라이브러리에 넘기지 않고 직접 소유한다.**

| 라이브러리에 맡기는 것                          | 직접 짜는 것 (라이브러리로 대체 금지)                     |
| ----------------------------------------------- | --------------------------------------------------------- |
| 캐시·dedup·`onMutate/onError/onSettled` 훅 지점 | **카드 단위 직렬 큐** — 같은 카드의 이동 요청을 순차화    |
| `cancelQueries`                                 | **`seq` 기반 stale 응답 폐기** — 늦게 도착한 옛 응답 무시 |
| 재시도 정책                                     | **필드 단위 롤백** — 전체 스냅샷 복원 금지                |

> **왜 전체 스냅샷 롤백이 틀렸나 (이 저장소의 핵심 함정)**
> TanStack Query 공식 optimistic update 레시피는 "동시에 실행되는 mutation이 하나"를 전제한다.
> 같은 카드를 빠르게 두 번 옮기면, 두 번째 `onMutate`가 **이미 낙관적으로 변경된 상태**를 스냅샷으로 잡는다.
> 이때 첫 번째가 실패해 첫 스냅샷으로 되돌리면 **한 단계 전이 아니라 두 단계 전으로 돌아간다.**
> → `moveQueue.ts`를 라이브러리 훅으로 치환하려는 리팩터링 제안은 **기각한다.**

### 반드시 지킬 구현 제약

- **MSW 저장소는 localStorage에 백킹한다.** 인메모리만 쓰면 새로고침 시 초기화되어 필수 요구사항("mock API에 저장되어 새로고침 후에도 유지")이 깨진다.
- **실패율·지연은 오버라이드 가능해야 한다** (URL 쿼리 / 환경변수). 테스트와 수동 검증에서 결정론적으로 실패를 주입해야 한다.
- **단계 이동은 액션 버튼 + 키보드 방식**이다 (드래그앤드롭 미채택). DnD 라이브러리를 추가하지 않는다.
- **접근성은 나중에 붙이는 게 아니라 기본값이다.** 새 인터랙션을 만들면 키보드 경로와 `aria-live` 안내를 같이 만든다.

### 설계 원칙

> **"실시간 갱신과 사용자 조작이 충돌하면 사용자 조작이 항상 이긴다."**
> 늦게 도착한 서버 응답이 사용자의 최신 조작을 덮어쓰지 않는다.

---

## 6. 커밋 규약

### AI는 커밋하지 않는다

```
1. 구현 + PROMPTS.md 섹션 작성 + 검증 수행
2. → 사람에게 보고
     · 변경 파일 목록
     · 확인해볼 시나리오 (구체적인 조작 순서)
     · 검증 결과 (codex 교차 리뷰가 무엇을 지적했는지 / 실행 검증에서 무엇이 나왔는지)
     · 커밋 메시지 초안
3. 사람이 직접 기능 점검
4. 사람이 "커밋해"라고 하면 → 그때 commit + push
```

승인 없이 커밋·푸시하지 않는다. **반복은 자동화하되 판단은 사람에게 남긴다.**

### 메시지 형식

```
type(scope): 요약

- 무엇을·왜
- AI 초안을 어떻게 손봤는지 한 줄   ← 필수. 6장 기록과 연결되어 평가에 반영된다
```

`type`: `feat` / `fix` / `refactor` / `test` / `docs` / `chore`

예시:

```
feat(optimistic-update): 카드 이동 낙관적 반영 + 실패 시 롤백

- 15% 실패에 대응해 onMutate 낙관 반영 / onError 복원 / onSettled 무효화
- AI 초안은 queryClient 전체 스냅샷을 복원 → 연속 이동 시 두 단계 전으로
  되돌아가는 문제. 필드 단위 롤백으로 교체
```

### 커밋 순서 (기능 단위 분해)

| #   | 커밋                        | 내용                                                         |
| --- | --------------------------- | ------------------------------------------------------------ |
| 0   | `docs(rules)`               | CLAUDE.md · AGENTS.md · PROGRESS.md · docs/PLAN.md           |
| 1   | `chore(setup)`              | Vite+React+TS, 린트, 폴더 구조, README 뼈대                  |
| 2   | `feat(mock-api)`            | MSW 핸들러 + localStorage persist + seed 1,000건 + 지연/실패 |
| 3   | `feat(board-layout)`        | 5단계 컬럼 레이아웃                                          |
| 4   | `feat(card-list)`           | 지원자 카드 (이름·직무·지원일·현재 단계)                     |
| 5   | `feat(loading-error-empty)` | 로딩 / 에러+재시도 / 빈 상태                                 |
| 6   | `feat(stage-move)`          | 액션 버튼 이동 + PATCH persist                               |
| 7   | `feat(optimistic-update)`   | 낙관적 반영 + 실패 롤백 + 피드백                             |
| 8   | `fix(optimistic-update)`    | 연속 이동 시 롤백이 두 단계 전으로 가던 문제                 |
| 9   | `feat(race-condition)`      | 카드별 직렬 큐 + seq 기반 stale 응답 폐기                    |
| 10  | `test(optimistic-rollback)` | 롤백·경쟁 상태·큐 순서 테스트                                |
| 11  | `feat(search-filter)`       | 이름 검색 + 직무 필터                                        |
| 12  | `feat(detail-panel)`        | 사이드 패널 상세                                             |
| 13  | `feat(a11y-keyboard)`       | 키보드 내비게이션 + aria-live + 포커스 관리                  |
| 14  | `feat(virtualization)`      | 컬럼 가상 스크롤 (1,000건)                                   |
| 15  | `feat(undo)`                | 되돌리기                                                     |
| 16  | `docs(decisions)`           | DECISIONS.md 정리                                            |
| 17  | `docs(readme)`              | 실행법·스택·성능 수치·배포 링크                              |

순서는 상황에 따라 바뀔 수 있다. **바뀌면 `PROGRESS.md`에 반영한다.**

---

## 7. 검증 절차

### 검증 강도는 기능마다 다르다

과제 6-3: _"AI가 완벽히 낸 기능이면 '1차 출력 그대로 채택, 엣지케이스 X만 직접 확인'처럼 짧아도 됩니다."_
전부에 풀 사이클을 도는 것은 오히려 형식적으로 보인다.

| 강도     | 대상                                                    | 절차                                                   |
| -------- | ------------------------------------------------------- | ------------------------------------------------------ |
| **풀**   | 7 낙관적 업데이트, 9 경쟁 상태, 13 a11y, 14 가상 스크롤 | 구현 → **codex 교차 리뷰** → **실행 검증** → 사람 판단 |
| **중**   | 2 mock API, 6 이동, 11 검색/필터, 12 상세 패널          | 구현 → 실행 검증 → 사람 판단                           |
| **경량** | 1 setup, 3 레이아웃, 4 카드, 5 상태 UI                  | 구현 → 코드 정독 → 사람 판단 (기록도 짧게)             |

### 교차 벤더 리뷰 — 왜 다른 벤더인가

같은 모델 계열에게 자기가 만든 걸 리뷰시키면 **자기 편향**이 생긴다. 자기가 놓친 건 다시 봐도 놓친다.
그래서 **다른 벤더의 CLI 도구(`codex`)를 호출**해 리뷰하게 한다. 사람 팀에서 코드 리뷰를 남에게 맡기는 것과 같은 이유다.

```bash
codex exec "다음 파일을 리뷰해줘: src/features/board/moveQueue.ts
맥락: 낙관적 업데이트 + 같은 카드 연속 이동 시 경쟁 상태를 처리하는 코드다.
mock API는 200~800ms 지연 + 15% 확률로 실패한다.
이 구현이 깨지는 시나리오를 찾아줘. 칭찬 말고 반박만 해줘."
```

**리뷰 결과를 그대로 수용하지 않는다.** 지적이 맞는지 직접 확인하고, 기각했다면 그 이유를 `PROMPTS.md`에 적는다.

### 실행 검증 — "완료했습니다"를 믿지 않는다

코드가 컴파일되는 것과 화면이 실제로 동작하는 것은 다르다.
**가장 위험한 실패는 에러가 나는 게 아니라 "됐다"고 말하는 것이다.**
(이 과제의 주제인 낙관적 업데이트가 정확히 그 문제다 — UI는 됐다고 말하는데 서버는 실패한 상태.)

Playwright MCP로 실제 화면을 띄워 조작한다. 시나리오:

1. 보드에 5개 컬럼 + 카드가 렌더되는가
2. 이동 버튼 클릭 시 **즉시** 다음 컬럼으로 옮겨지는가 (낙관 반영)
3. 실패율 100% 강제(`?failRate=1`) → 카드 원위치 + 에러 피드백 + `aria-live` 안내
4. 성공 후 **새로고침 → 이동 상태 유지** (localStorage persist)
5. 같은 카드 3연속 이동 → 최종 단계가 마지막 조작과 일치. 중간 실패가 다른 조작을 덮지 않음
6. 1,000건에서 이름 검색 타이핑 → 입력 지연 없음
7. 마우스 없이 Tab / ←→ / Enter / Esc 만으로 전 시나리오 재현
8. 1,000건에서 컬럼 DOM 노드 수 확인 (가상 스크롤 전/후 수치를 README에 기록)
9. 빈 검색 결과 → 빈 상태 UI / 네트워크 실패 → 에러 상태 + 재시도

### 자동 검증

```bash
npx tsc --noEmit    # 에러 0
npm test            # 롤백·경쟁 상태 테스트 통과
npm run build       # 빌드 성공
```

---

## 8. 기록 문서

| 파일           | 무엇을 적나                                                               |
| -------------- | ------------------------------------------------------------------------- |
| `PROMPTS.md`   | **기능별** 프롬프트 원문 + AI 출력 요지 + 리뷰/검증. 커밋과 1:1로 대응    |
| `DECISIONS.md` | 설계 결정 3~5개 + 트레이드오프 + **확정한 가정** + 못 끝낸 기능과 그 이유 |
| `PROGRESS.md`  | 진행 상태. 매 작업마다 갱신                                               |
| `README.md`    | 실행법·스택·mock API 방식·구현 범위·키보드 조작표·성능 수치·배포 링크     |
| `docs/PLAN.md` | 전체 수행 계획 (착수 시점에 작성)                                         |

### `PROMPTS.md` 섹션 형식

```markdown
## [stage-move] 카드 단계 이동

### 프롬프트 1

(AI에 준 지시 — 요약이 아니라 실제 문구)

### AI 출력 요지

(무엇을 내놓았나)

### 리뷰 / 검증 ← 이 섹션이 기록의 핵심이다

- 무엇이 문제였나 (틀림 · 비효율 · 요구 누락)
- 어떻게 알아챘나 (직접 재현 · 테스트 · 정독 · codex 교차 리뷰)
- 그대로 채택 / 수정 / 기각 → 어떤 판단을 왜 했나
- (수정했다면) 어떻게 고쳤나
```

**길이가 아니라 판단의 밀도를 본다.** 프롬프트 원문은 붙이되, 리뷰/검증은 **실제로 검증한 내용만** 진솔하게 적는다.

---

## 9. 새 세션에서 이어서 작업할 때

```
CLAUDE.md와 PROGRESS.md를 읽고, "지금 하던 것"부터 이어서 진행해줘. 커밋은 하지 말고.
```

작업 전 확인:

- [ ] `PROGRESS.md`의 「지금 하던 것」과 「열린 이슈」를 읽었다
- [ ] `PROMPTS.md`에서 직전 기능의 기록 형식을 확인했다 (세션이 바뀌어도 형식이 흔들리지 않게)
- [ ] `git log --oneline`으로 커밋 순서가 §6 표와 어긋나지 않는지 확인했다
