# AGENTS.md

> **규칙 정본은 [`CLAUDE.md`](./CLAUDE.md)다. 작업 전에 반드시 읽을 것.**
> 이 파일은 Codex CLI · Cursor 등 다른 도구가 진입점으로 읽는 포인터이며, 규칙을 두 벌로 유지하지 않는다.

## 읽는 순서

1. [`CLAUDE.md`](./CLAUDE.md) — 규칙 정본
2. [`PROGRESS.md`](./PROGRESS.md) — 지금 어디까지 했는지
3. [`docs/PLAN.md`](./docs/PLAN.md) — 전체 수행 계획 (필요할 때)

## 잊지 말아야 할 6가지 (전문은 CLAUDE.md)

1. **커밋하지 마라.** 파일만 만들고 보고한다. 커밋·푸시는 사람이 판단한다.
2. **히스토리를 건드리지 마라.** `rebase -i` / `squash` / `--amend` / `push --force` / `reset --hard` 금지.
   커밋 히스토리는 제출물의 일부이고, 시행착오 흔적도 그 과정의 기록이다.
3. **기능 하나 = 커밋 하나 = 기록 한 섹션.** 여러 기능을 한 커밋에 섞지 않는다.
4. **`PROMPTS.md`의 리뷰/검증을 지어내지 마라.** 실제로 실행·재현·리뷰한 것만 적는다.
   검증하지 않았으면 "검증하지 않음"이라고 적는다. 기록 하나가 거짓이면 나머지도 믿을 수 없다.
5. **모호하면 추측하지 말고 물어라.** 단, 질문은 한 번에 묶어서 한다.
6. **작성자가 설명할 수 없는 코드는 남기지 마라.** 넘기기 전에 왜 이렇게 짰는지 함께 설명한다.

## 이 저장소에서 자주 잘못 건드리는 것

- `src/features/board/moveQueue.ts` — 카드별 직렬 큐 + seq 기반 stale 응답 폐기.
  **TanStack Query 훅으로 대체하자는 제안은 기각한다.** 이유는 CLAUDE.md §5 참조.
- `src/mocks/db.ts` — MSW 저장소는 **localStorage 백킹이 필수**다. 인메모리로 되돌리면 필수 요구사항이 깨진다.
- 실패율·지연 오버라이드(`?failRate=`) — 검증에 필요하다. 제거하지 않는다.

## 이 저장소를 리뷰할 때 (codex 교차 리뷰로 호출된 경우)

당신은 **자기 편향을 깨기 위해** 불려왔다. 다른 모델이 작성한 코드를 반박하는 것이 역할이다.

- **칭찬하지 마라.** 깨지는 시나리오를 찾는 것이 목적이다.
- 특히 봐야 할 것: mock API가 200~800ms 지연 + 15% 확률로 실패한다는 전제에서,
  **동시성 / 늦게 도착한 응답 / 롤백 대상 / 포커스 소실 / 스크린리더 안내 누락**이 어떻게 깨지는가.
- 추측이면 추측이라고 밝혀라. 확인 가능한 근거(파일·라인·재현 절차)를 함께 제시하라.
